# 校园旧书漂流共享系统 - 完整项目分析（下篇）

---

## 五、图书类型管理模块

### 5.1 模块概述
图书类型管理模块主要负责图书分类的增删改查功能，支持管理员对图书分类进行管理，方便用户按类型浏览和查找图书。

### 5.2 核心代码文件分析

#### 5.2.1 实体类

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/model/BookType.java

```java
package com.shanzhu.book.model;

import lombok.Data;

/**
 * 地址相关
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
@Data
public class BookType {

    /**
     * 图书类型id
     */
    private Integer booktypeid;

    /**
     * 图书类型名
     */
    private String booktypename;

    /**
     * 图书类型描述
     */
    private String booktypedesc;

}
```

#### 5.2.2 数据访问层（Mapper）

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/mapper/BookTypeMapper.java

```java
package com.shanzhu.book.mapper;

import com.shanzhu.book.model.BookType;
import org.apache.ibatis.annotations.Param;

import java.util.List;
import java.util.Map;

/**
 * 图书类型 持久层
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
public interface BookTypeMapper {

    int deleteByPrimaryKey(Integer booktypeid);

    int insert(BookType record);

    int insertSelective(BookType record);

    BookType selectByPrimaryKey(Integer booktypeid);

    int updateByPrimaryKeySelective(BookType record);

    int updateByPrimaryKey(BookType record);

    List<BookType> selectAllByLimit(@Param("begin") Integer begin, @Param("size") Integer size);

    Integer selectCount();

    List<BookType> selectAll();

    int selectCountBySearch(Map<String, Object> searchParam);

    List<BookType> selectBySearch(Map<String, Object> searchParam);
}
```

#### 5.2.3 服务层接口

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/service/BookTypeService.java

```java
package com.shanzhu.book.service;

import com.shanzhu.book.model.BookType;

import java.util.List;
import java.util.Map;

/**
 * 图书类型 服务层
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
public interface BookTypeService {

    Integer getCount();

    Integer getSearchCount(Map<String, Object> params);

    List<BookType> searchBookTypesByPage(Map<String, Object> params);

    Integer addBookType(BookType bookType);

    Integer deleteBookType(BookType bookType);

    Integer deleteBookTypes(List<BookType> bookTypes);

    Integer updateBookType(BookType bookType);

    List<BookType> queryBookTypes();

}
```

#### 5.2.4 服务层实现

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/service/impl/BookTypeServiceImpl.java

```java
package com.shanzhu.book.service.impl;

import com.shanzhu.book.mapper.BookInfoMapper;
import com.shanzhu.book.mapper.BookTypeMapper;
import com.shanzhu.book.model.BookType;
import com.shanzhu.book.service.BookTypeService;
import org.springframework.stereotype.Service;

import javax.annotation.Resource;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

@Service
public class BookTypeServiceImpl implements BookTypeService {

    @Resource
    private BookTypeMapper bookTypeMapper;

    @Resource
    private BookInfoMapper bookInfoMapper;

    @Override
    public Integer getCount() {
        return bookTypeMapper.selectCount();
    }

    @Override
    public Integer getSearchCount(Map<String, Object> params) {
        return bookTypeMapper.selectCountBySearch(params);
    }

    @Override
    public List<BookType> searchBookTypesByPage(Map<String, Object> params) {
        return bookTypeMapper.selectBySearch(params);
    }

    @Override
    public Integer addBookType(BookType bookType) {
        if (bookType.getBooktypedesc() == null) {
            bookType.setBooktypedesc("");
        }
        return bookTypeMapper.insertSelective(bookType);
    }

    @Override
    public Integer deleteBookType(BookType bookType) {
        int count = 0;
        try{
            Map<String, Object> map = new HashMap<>();
            map.put("booktypeid", bookType.getBooktypeid());
            if(bookInfoMapper.selectCountByType(map) > 0) {
                return -1;
            }
            count = bookTypeMapper.deleteByPrimaryKey(bookType.getBooktypeid());
        } catch (Exception e) {
            e.printStackTrace();
        }
        return count;
    }

    @Override
    public Integer deleteBookTypes(List<BookType> bookTypes) {
        int count = 0;
        for(BookType bookType : bookTypes) {
            count += deleteBookType(bookType);
        }
        return count;
    }

    @Override
    public Integer updateBookType(BookType bookType) {
        return bookTypeMapper.updateByPrimaryKeySelective(bookType);
    }

    @Override
    public List<BookType> queryBookTypes() {
        return bookTypeMapper.selectAll();
    }
}
```

**关键业务逻辑说明：**
1. `deleteBookType()` 方法实现了删除时的安全检查，如果该类型下还有图书，则返回 -1 表示不能删除
2. `addBookType()` 方法对描述字段进行了空值处理

#### 5.2.5 控制器层

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/web/BookTypeController.java

```java
package com.shanzhu.book.web;

import com.shanzhu.book.model.BookType;
import com.shanzhu.book.service.BookTypeService;
import com.shanzhu.book.utils.PageUtils;
import com.shanzhu.book.utils.R;
import org.springframework.web.bind.annotation.*;

import javax.annotation.Resource;
import java.util.List;
import java.util.Map;

/**
 * 图书类型 控制层
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
@RestController
@RequestMapping(value = "/bookType")
public class BookTypeController {

    @Resource
    private BookTypeService bookTypeService;

    /**
     * 获得数量
     *
     * @return 数量
     */
    @GetMapping(value = "/getCount")
    public Integer getCount(){
        return bookTypeService.getCount();
    }

    /**
     * 查询所有图书类型
     *
     * @return 图书类型
     */
    @GetMapping(value = {"/queryBookTypes", "/reader/queryBookTypes"})
    public List<BookType> queryBookTypes(){
        return bookTypeService.queryBookTypes();
    }

    /**
     * 分页查询图书类型
     *
     * @param params {page, limit, booktypename}
     * @return 图书类型
     */
    @GetMapping(value = "/queryBookTypesByPage")
    public Map<String, Object> queryBookTypesByPage(@RequestParam Map<String, Object> params){
        PageUtils.parsePageParams(params);
        int count = bookTypeService.getSearchCount(params);
        List<BookType> bookTypes = bookTypeService.searchBookTypesByPage(params);
        return R.getListResultMap(0, "success", count, bookTypes);
    }

    /**
     * 添加类型
     *
     * @param bookType 图书类型
     * @return 结果
     */
    @PostMapping(value = "/addBookType")
    public Integer addBookType(@RequestBody BookType bookType){
        return bookTypeService.addBookType(bookType);
    }

    /**
     * 删除类型
     *
     * @param bookType 图书类型
     * @return 结果
     */
    @DeleteMapping(value = "/deleteBookType")
    public Integer deleteBookType(@RequestBody BookType bookType){
        return bookTypeService.deleteBookType(bookType);
    }

    /**
     * 批量删除类型
     *
     * @param bookTypes 图书类型
     * @return 结果
     */
    @DeleteMapping(value = "/deleteBookTypes")
    public Integer deleteBookTypes(@RequestBody List<BookType> bookTypes){
        return bookTypeService.deleteBookTypes(bookTypes);
    }

    /**
     * 更新类型
     *
     * @param bookType 图书类型
     * @return 结果
     */
    @PutMapping(value = "/updateBookType")
    public Integer updateBookType(@RequestBody BookType bookType){
        return bookTypeService.updateBookType(bookType);
    }

}
```

**API接口说明：**
- `GET /bookType/getCount`：获取图书类型总数
- `GET /bookType/queryBookTypes` 和 `GET /bookType/reader/queryBookTypes`：查询所有图书类型（管理员和普通用户都可访问）
- `GET /bookType/queryBookTypesByPage`：分页查询图书类型
- `POST /bookType/addBookType`：添加图书类型
- `DELETE /bookType/deleteBookType`：删除单个图书类型
- `DELETE /bookType/deleteBookTypes`：批量删除图书类型
- `PUT /bookType/updateBookType`：更新图书类型

#### 5.2.6 前端API封装

**【完整文件相对路径】** 代码/book-frontend/src/api/booktype.js

```javascript
import request from '@/utils/request'

// 获取图书类型数量
export function getCount() {
    return request({
        url: '/bookType/getCount',
        method: 'get'
    })
}

// 查询所有图书类型
export function queryBookTypes() {
    return request({
        url: '/bookType/queryBookTypes',
        method: 'get'
    })
}

// 分页查询图书类型
export function queryBookTypesByPage(params) {
    return request({
        url: '/bookType/queryBookTypesByPage',
        method: 'get',
        params
    })
}

// 添加图书类型
export function addBookType(data) {
    return request({
        url: '/bookType/addBookType',
        method: 'post',
        data
    })
}

// 删除图书类型
export function deleteBookType(data) {
    return request({
        url: '/bookType/deleteBookType',
        method: 'delete',
        data
    })
}

//  删除一些图书类型
export function deleteBookTypes(data) {
    return request({
        url: '/bookType/deleteBookTypes',
        method: 'delete',
        data
    })
}

//  更新图书类型
export function updateBookType(data) {
    return request({
        url: '/bookType/updateBookType',
        method: 'put',
        data
    })
}
```

---

## 六、心愿书单管理模块

### 6.1 模块概述
心愿书单模块允许用户发布自己想要的图书需求，其他用户看到后如果有这本书可以进行认领，帮助完成心愿。

### 6.2 核心代码文件分析

#### 6.2.1 实体类

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/model/BookWish.java

```java
package com.shanzhu.book.model;

import com.fasterxml.jackson.annotation.JsonFormat;
import lombok.Data;
import java.util.Date;

@Data
public class BookWish {
    private Integer wishId;
    private Integer userId;
    private String userName;
    private String bookName;
    private String wishDesc;
    private Integer state;
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
    private Date createTime;
    private Integer fulfillUserId;
}
```

**字段说明：**
- `state`：心愿状态，0表示未完成，1表示已完成
- `fulfillUserId`：完成心愿的用户ID

#### 6.2.2 数据访问层（Mapper）

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/mapper/BookWishMapper.java

```java
package com.shanzhu.book.mapper;

import com.shanzhu.book.model.BookWish;
import org.apache.ibatis.annotations.*;
import java.util.List;

@Mapper
public interface BookWishMapper {
    @Insert("INSERT INTO book_wish(user_id, user_name, book_name, wish_desc, state, create_time) " +
            "VALUES(#{userId}, #{userName}, #{bookName}, #{wishDesc}, 0, NOW())")
    int insertWish(BookWish wish);

    @Update("UPDATE book_wish SET state = 1, fulfill_user_id = #{fulfillUserId} WHERE wish_id = #{wishId}")
    int fulfillWish(@Param("wishId") Integer wishId, @Param("fulfillUserId") Integer fulfillUserId);

    @Select("SELECT wish_id as wishId, user_id as userId, user_name as userName, " +
            "book_name as bookName, wish_desc as wishDesc, state, " +
            "create_time as createTime, fulfill_user_id as fulfillUserId " +
            "FROM book_wish ORDER BY state ASC, create_time DESC")
    List<BookWish> selectAllWishes();

    @Delete("DELETE FROM book_wish WHERE wish_id = #{wishId}")
    int deleteWish(@Param("wishId") Integer wishId);


    // 暂时改为精确匹配，测试一下是否能跑通
    @Select("SELECT wish_id as wishId, user_id as userId, user_name as userName, " +
            "book_name as bookName, wish_desc as wishDesc, state, " +
            "create_time as createTime, fulfill_user_id as fulfillUserId " +
            "FROM book_wish WHERE state = 0 AND book_name = #{bookName}")
    List<BookWish> selectUnfulfilledWishesByBookName(@Param("bookName") String bookName);

}
```

**关键SQL说明：**
- 使用注解方式直接编写SQL，无需XML配置
- `selectUnfulfilledWishesByBookName()`：精确匹配未完成的心愿，方便用户发布图书时检查是否有匹配的心愿

#### 6.2.3 控制器层

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/web/BookWishController.java

```java
package com.shanzhu.book.web;

import com.shanzhu.book.mapper.BookWishMapper;
import com.shanzhu.book.model.BookWish;
import org.springframework.web.bind.annotation.*;
import javax.annotation.Resource;
import java.util.List;

@RestController
@RequestMapping("/wish")
public class BookWishController {

    @Resource
    private BookWishMapper bookWishMapper;

    @PostMapping("/add")
    public Integer addWish(@RequestBody BookWish wish) {
        return bookWishMapper.insertWish(wish);
    }

    @GetMapping("/list")
    public List<BookWish> getWishList() {
        return bookWishMapper.selectAllWishes();
    }

    @PostMapping("/fulfill")
    public Integer fulfillWish(Integer wishId, Integer fulfillUserId) {
        return bookWishMapper.fulfillWish(wishId, fulfillUserId);
    }

    @PostMapping("/delete")
    public Integer deleteWish(Integer wishId) {
        return bookWishMapper.deleteWish(wishId);
    }
}
```

**API接口说明：**
- `POST /wish/add`：添加心愿
- `GET /wish/list`：获取心愿列表
- `POST /wish/fulfill`：完成心愿
- `POST /wish/delete`：删除心愿

#### 6.2.4 前端API封装

**【完整文件相对路径】** 代码/book-frontend/src/api/wish.js

```javascript
import request from '@/utils/request'

export function getWishList() { return request({ url: '/wish/list', method: 'get' }) }
export function addWish(data) { return request({ url: '/wish/add', method: 'post', data }) }
export function fulfillWish(wishId, fulfillUserId) { return request({ url: '/wish/fulfill', method: 'post', params: { wishId, fulfillUserId } }) }
export function deleteWish(wishId) { return request({ url: '/wish/delete', method: 'post', params: { wishId } }) }
```

---

## 七、消息通知模块

### 7.1 模块概述
消息通知模块用于向用户发送系统通知，包括借阅状态变更、审核结果等重要信息。

### 7.2 核心代码文件分析

#### 7.2.1 实体类

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/model/Message.java

```java
package com.shanzhu.book.model;

import lombok.Data;
import java.io.Serializable;
import java.util.Date;

@Data
public class Message implements Serializable {
    private Integer messageid;
    private Integer userid;
    private String content;
    private Integer isread;
    private Date createtime;

    // 构造函数方便使用
    public Message() {}

    public Message(Integer userid, String content) {
        this.userid = userid;
        this.content = content;
        this.isread = 0;
        this.createtime = new Date();
    }
}
```

#### 7.2.2 数据访问层（Mapper）

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/mapper/MessageMapper.java

```java
package com.shanzhu.book.mapper;

import com.shanzhu.book.model.Message;
import org.apache.ibatis.annotations.Param;
import java.util.List;

public interface MessageMapper {
    int insert(Message record);

    // 查询某用户的消息列表
    List<Message> selectByUserId(@Param("userid") Integer userid);

    // 标记消息为已读（可选功能）
    int updateReadStatus(@Param("messageid") Integer messageid);
}
```

#### 7.2.3 控制器层

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/web/MessageController.java

```java
package com.shanzhu.book.web;

import com.shanzhu.book.mapper.MessageMapper;
import com.shanzhu.book.model.Message;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/message")
public class MessageController {

    @Autowired
    private MessageMapper messageMapper;

    /**
     * 根据前端传来的用户ID，拉取该用户的所有站内消息
     * 对应前端请求：/message/getByUserId?userid=xxx
     */
    @GetMapping("/getByUserId")
    public List<Message> getByUserId(@RequestParam("userid") Integer userid) {
        // 调用你现有的 Mapper 获取数据
        return messageMapper.selectByUserId(userid);
    }

    /**
     * (保留备用) 后续如果管理员审核通过后需要下发消息，可以调用此接口
     */
    @PostMapping("/send")
    public int sendMessage(@RequestBody Message message) {
        return messageMapper.insert(message);
    }
}
```

**API接口说明：**
- `GET /message/getByUserId`：获取指定用户的消息列表
- `POST /message/send`：发送消息（备用接口）

---

## 八、文件上传模块

### 8.1 模块概述
文件上传模块用于处理图书封面图片的上传功能，将图片保存到服务器指定目录并返回访问路径。

### 8.2 核心代码文件分析

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/web/uploadController.java

```java
package com.shanzhu.book.web;

import lombok.extern.slf4j.Slf4j;
import org.apache.tomcat.util.http.fileupload.FileItem;
import org.apache.tomcat.util.http.fileupload.FileItemFactory;
import org.apache.tomcat.util.http.fileupload.disk.DiskFileItemFactory;
import org.apache.tomcat.util.http.fileupload.servlet.ServletFileUpload;
import org.apache.tomcat.util.http.fileupload.servlet.ServletRequestContext;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

import javax.servlet.http.HttpServletRequest;
import java.io.File;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

@Slf4j
@Controller
@RequestMapping("/upload")
public class uploadController {

    @RequestMapping("/uploadImg")
    @ResponseBody
    public Map<String, Object> uploadImg(HttpServletRequest req) {
        String resPath = upload(req);
        Map<String, Object> res = new HashMap<>();
        res.put("code", 0);
        res.put("data", resPath);
        return res;
    }

    private String upload(HttpServletRequest req) {
        String res = null;
        try {
            // 【终极方案】：直接写死你的电脑绝对路径（注意 Java 中路径要用正斜杠 / ）
            String staticDir = "E:/java/project/校园旧书漂流共享系统/代码/book-backend/src/main/resources/static/files/";
            File resDirFile = new File(staticDir);
            if (!resDirFile.exists()) {
                resDirFile.mkdirs();
            }

            if (ServletFileUpload.isMultipartContent(req)) {
                FileItemFactory fileItemFactory = new DiskFileItemFactory();
                ServletFileUpload servletFileUpload = new ServletFileUpload(fileItemFactory);
                List<FileItem> list = servletFileUpload.parseRequest(new ServletRequestContext(req));
                for (FileItem fileItem : list) {
                    if (!fileItem.isFormField()) {
                        String newFileName = System.currentTimeMillis() + "_" + fileItem.getName();
                        File file = new File(staticDir + newFileName);
                        fileItem.write(file);
                        // 返回给前端的相对地址
                        res = "/files/" + newFileName;
                    }
                }
            }
        } catch (Exception e) {
            log.error("文件上传失败！", e);
        }
        return res;
    }
}
```

**关键实现说明：**
1. 使用 Apache Commons FileUpload 处理文件上传
2. 文件保存到指定的绝对路径
3. 文件名使用时间戳前缀避免重名
4. 返回给前端的是相对访问路径 `/files/文件名`

---

## 九、系统配置与异常处理模块

### 9.1 跨域配置

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/config/CorsConfig.java

```java
package com.shanzhu.book.config;

import com.shanzhu.book.utils.PathUtils;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/**
 * web跨域相关配置
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
@Configuration
public class CorsConfig implements WebMvcConfigurer {

    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                //是否发送Cookie
                .allowCredentials(true)
                //设置放行哪些原始域   SpringBoot2.4.4下低版本使用.allowedOrigins("*")
                .allowedOriginPatterns("*")
                //放行哪些请求方式
                .allowedMethods("GET", "POST", "PUT", "DELETE")
                //.allowedMethods("*") //或者放行全部
                //暴露哪些原始请求头部信息
                .allowedHeaders("*");
    }

    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        String winPath = PathUtils.getClassLoadRootPath() + "/src/main/resources/static/files/";

        //第一个方法设置访问路径前缀，第二个方法设置资源路径
        registry.addResourceHandler("/files/**").addResourceLocations("file:" + winPath);
        WebMvcConfigurer.super.addResourceHandlers(registry);
    }
}
```

**功能说明：**
1. 配置跨域支持，允许前后端分离架构
2. 配置静态资源映射，使上传的图片可以通过 `/files/**` 路径访问

### 9.2 静态资源配置

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/config/WebMvcConfig.java

```java
package com.shanzhu.book.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebMvcConfig implements WebMvcConfigurer {

    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        // 【终极方案】：直接映射到你的物理绝对路径
        String absolutePath = "file:E:/java/project/校园旧书漂流共享系统/代码/book-backend/src/main/resources/static/files/";

        registry.addResourceHandler("/files/**")
                .addResourceLocations(absolutePath);
    }
}
```

### 9.3 数据库迁移配置

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/config/DbMigrator.java

```java
package com.shanzhu.book.config;

import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.jdbc.core.JdbcTemplate;
import lombok.extern.slf4j.Slf4j;

@Configuration
@Slf4j
public class DbMigrator {

    @Bean
    public CommandLineRunner migrate(JdbcTemplate jdbcTemplate) {
        return args -> {
            try {
                log.info("Checking database schema for book_info.auditStatus...");
                // DEFAULT 1 means existing records will act as already approved.
                jdbcTemplate.execute("ALTER TABLE book_info ADD COLUMN auditStatus INT DEFAULT 1 COMMENT '审核状态 0:待审 1:通过 2:驳回'");
                log.info("Successfully added auditStatus column to book_info table.");
            } catch (Exception e) {
                // If the column already exists, this exception will be caught. We can safely ignore it.
                if (e.getMessage() != null && e.getMessage().contains("Duplicate column name")) {
                    log.info("Column auditStatus already exists in book_info, skip migration.");
                } else {
                    log.error("Database migration executed with warnings: {}", e.getMessage());
                }
            }
        };
    }
}
```

**功能说明：**
- 应用启动时自动检查并添加 `auditStatus` 字段
- 如果字段已存在则跳过，避免重复执行报错
- 实现数据库结构的自动升级

### 9.4 用户拦截器

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/interceptor/UserInterceptor.java

```java
package com.shanzhu.book.interceptor;

import com.shanzhu.book.model.User;
import org.springframework.web.servlet.HandlerInterceptor;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 * 用户拦截器，拦截未登录不能访问的请求
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
public class UserInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
        User user = (User) request.getSession().getAttribute("userObj");

        if (user == null) {  //没有登录
            System.out.println("没有登录!不能访问!");
            // 重定向到登录界面
            response.sendRedirect(request.getContextPath() + "/index.html");
            return false;
        }

        //放行
        return true;
    }

}
```

**功能说明：**
- 检查用户是否登录
- 未登录用户重定向到登录页面

### 9.5 管理员拦截器

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/interceptor/ReaderInterceptor.java

```java
package com.shanzhu.book.interceptor;

import com.shanzhu.book.model.User;
import org.springframework.web.servlet.HandlerInterceptor;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 * 读者拦截器：拦截读者不能访问的请求
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
public class ReaderInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
        User user = (User) request.getSession().getAttribute("userObj");

        // 根据链式拦截关系，到这个拦截器肯定是登录过的。
        if (user.getIsadmin() == 0) {    //如果是用户，则拦截
            System.out.println("读者不能进管理员界面!");
            // 重定向到登录界面
            response.sendRedirect(request.getContextPath() + "/index.html");
            return false;
        }

        //放行
        return true;
    }
}
```

**功能说明：**
- 检查用户是否为管理员
- 普通用户无法访问管理员功能

### 9.6 全局异常处理器

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/exception/GlobalExceptionHandler.java

```java
package com.shanzhu.book.exception;

import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ControllerAdvice;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.ResponseBody;

/**
 * 全局异常处理器
 * 系统中抛出的未处理异常，统一都在这里做处理
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
@ControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(value = Exception.class)
    @ResponseBody
    public ResponseEntity<String> handleException(Exception e) {
        // 自定义异常处理逻辑
        String message = e.getMessage();
        e.printStackTrace();
        if (message.contains("(using password: YES)")) {
            if (!message.contains("'root'@'")) {
                message = "PU Request failed with status code 500";
            } else if (message.contains("'root'@'localhost'")) {
                message = "P Request failed with status code 500";
            }
        } else if (message.contains("Unknown database")) {
            message = "U Request failed with status code 500";
        } else if (message.contains("edis")) {
            message = "R Request failed with status code 500";
        } else if (message.contains("Failed to obtain JDBC Connection")) {
            message = "C Request failed with status code 500";
        } else if (message.contains("SQLSyntaxErrorException")) {
            message = "S Request failed with status code 500";
        }
        return new ResponseEntity<>(message, HttpStatus.INTERNAL_SERVER_ERROR);
    }
}
```

**功能说明：**
- 统一处理系统中的未捕获异常
- 对不同类型的错误进行分类处理和信息脱敏
- 返回友好的错误信息给前端

---

## 十、工具类模块

### 10.1 分页工具类

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/utils/PageUtils.java

```java
package com.shanzhu.book.utils;

import java.util.Map;

public class PageUtils {

    public static void parsePageParams(Map<String, Object> params) {
        int page = Integer.parseInt((String) params.get("page"));
        int size = Integer.parseInt((String) params.get("limit"));
        page = Math.max(page, 1);
        params.put("begin", (page - 1) * size);
        params.put("size", size);
    }

}
```

**功能说明：**
- 将前端传来的 `page` 和 `limit` 参数转换为数据库查询需要的 `begin`（偏移量）和 `size`（每页大小）

### 10.2 路径工具类

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/utils/PathUtils.java

```java
package com.shanzhu.book.utils;

import java.io.UnsupportedEncodingException;
import java.net.URLDecoder;

/**
 * 路径处理工具类
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
public class PathUtils {
    public static String getClassLoadRootPath() {
        String path = "";
        try {
            String prePath = URLDecoder.decode(PathUtils.class.getClassLoader().getResource("").getPath(),"utf-8").replace("/target/classes", "");
            String osName = System.getProperty("os.name");
            if (osName.toLowerCase().startsWith("mac")) {
                // 苹果
                path = prePath.substring(0, prePath.length() - 1);
            } else if (osName.toLowerCase().startsWith("windows")) {
                // windows
                path = prePath.substring(1, prePath.length() - 1);
            } else if(osName.toLowerCase().startsWith("linux") || osName.toLowerCase().startsWith("unix")) {
                // unix or linux
                path = prePath.substring(0, prePath.length() - 1);
            } else {
                path = prePath.substring(1, prePath.length() - 1);
            }
        } catch (UnsupportedEncodingException e) {
            e.printStackTrace();
        }
        return path;
    }

}
```

**功能说明：**
- 获取项目根路径
- 跨平台支持（Mac、Windows、Linux）

### 10.3 JSON工具类

**【完整文件相对路径】** 代码/book-backend/src/main/java/com/shanzhu/book/utils/JsonUtil.java

```java
package com.shanzhu.book.utils;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationFeature;
import com.fasterxml.jackson.databind.JavaType;
import com.fasterxml.jackson.databind.ObjectMapper;

import java.io.IOException;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.io.Reader;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

/**
 * Json工具类
 *
 * @author: ShanZhu
 * @date: 2023-12-31
 */
public class JsonUtil {
    static ObjectMapper objectMapper = new ObjectMapper();
    static {
        objectMapper.configure(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false);// 允许pojo中有在json串中不存在的字段
        objectMapper.configure(JsonParser.Feature.ALLOW_COMMENTS, true);// 允许有注释
    }

    public static <T>T parseObject(InputStream inputStream, Class<T> tClass)  {
        Reader reader = new InputStreamReader(inputStream);
        try {
            return objectMapper.readValue(reader, tClass);
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }

    public static <T>T parseObject(String json,Class<T> tClass){
        try {
            return objectMapper.readValue(json,tClass);
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }

    public static <T>List<T> parseList(String json,Class<T> tClass) {
        JavaType javaType = objectMapper.getTypeFactory().constructParametricType(List.class, tClass);
        try {
            List<T> list  = objectMapper.readValue(json, javaType);
            return list;
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }

    public static <K,V>Map<K,V> parseMap(String json, Class<K> tclass1, Class<V> tclass2) {
        JavaType javaType = objectMapper.getTypeFactory().constructParametricType(HashMap.class,tclass1,tclass2);
        try {
            Map<K,V> map  = objectMapper.readValue(json, javaType);
            return map;
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }

    public static String toJsonString(Object object){
        try {
            return objectMapper.writeValueAsString(object);
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }

}
```

**功能说明：**
- JSON与Java对象的相互转换
- 支持对象、列表、Map等多种类型
- 配置了忽略未知属性和允许注释的特性

---

## 十一、数据看板模块

### 11.1 前端API封装

**【完整文件相对路径】** 代码/book-frontend/src/api/dashboard.js

```javascript
import request from '@/utils/request'
import { queryBookTypes } from '@/api/booktype'
import { queryBookInfosByPage, queryBookInfos } from '@/api/bookinfo'
import { queryBorrowsByPage } from '@/api/borrow'

// 获取图书类型分布数据
export async function getBookTypeDistribution() {
  try {
    const types = await queryBookTypes()

    const result = await Promise.all(types.map(async (type) => {
      const params = {
        page: 1,
        limit: 1,
        booktypeid: type.booktypeid
      }
      const response = await queryBookInfosByPage(params)
      return {
        name: type.booktypename,
        value: response.count || 0
      }
    }))

    return result
  } catch (error) {
    console.error('获取图书类型分布数据失败:', error)
    return []
  }
}

// 【核心修复】获取借阅状态分布数据
export async function getBorrowStatusDistribution() {
  try {
    const books = await queryBookInfos()

    let borrowedCount = 0 // 漂流中
    let availableCount = 0 // 在馆

    books.forEach(book => {
      // 容错处理：确保是数字
      const total = book.bookcount || 1;
      const current = book.inventory !== undefined ? book.inventory : 1;

      // 在馆 = 当前库存
      availableCount += current;
      // 漂流中 = 总数 - 库存
      const borrowed = total - current;
      if(borrowed > 0) {
        borrowedCount += borrowed;
      }
    })

    return {
      categories: ['在馆图书', '漂流中图书'],
      values: [availableCount, borrowedCount]
    }
  } catch (error) {
    console.error('获取借阅状态分布数据失败:', error)
    return {
      categories: [],
      values: []
    }
  }
}

// 获取高价图书排行榜
export async function getHighPriceBooks() {
  try {
    const books = await queryBookInfos()

    const pricedBooks = books
      .filter(book => book.bookname && book.bookprice)
      .map(book => ({
        name: book.bookname,
        value: parseFloat(book.bookprice) || 0
      }))
      .sort((a, b) => b.value - a.value)
      .slice(0, 6)

    return {
      books: pricedBooks.map(book => book.name),
      values: pricedBooks.map(book => book.value)
    }
  } catch (error) {
    console.error('获取图书价格统计数据失败:', error)
    return {
      books: [],
      values: []
    }
  }
}
```

**功能说明：**
1. `getBookTypeDistribution()`：获取各类型图书数量分布
2. `getBorrowStatusDistribution()`：获取在馆和漂流中图书数量
3. `getHighPriceBooks()`：获取高价图书排行榜（前6名）

---

## 十二、项目功能联动与数据流转说明

### 12.1 整体架构流程

本项目采用典型的前后端分离架构，数据流转主要分为以下几个核心环节：

```
用户操作 → 前端Vue页面 → 前端API封装 → HTTP请求 → 后端Controller → Service层 → Mapper层 → MySQL数据库
                                              ↓
                                        返回响应数据
                                              ↓
                                        前端页面更新
```

### 12.2 用户注册与登录流程

#### 12.2.1 用户注册流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-frontend/src/views/register/index.vue
2. 【完整文件相对路径】代码/book-frontend/src/api/user.js
3. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/UserController.java
4. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/service/impl/UserServiceImpl.java
5. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/mapper/UserMapper.java

**完整代码（关键部分）：**

**前端注册页面（register/index.vue 关键代码）：**
```javascript
// 调用 register API
import { register } from '@/api/user'

handleRegister() {
  register(this.registerForm).then(response => {
    this.$message.success('注册成功！请登录')
    this.activeName = 'login'
  })
}
```

**前端API封装（user.js）：**
```javascript
export function register(data) {
    return request({
        url: '/user/register',
        method: 'post',
        data
    })
}
```

**后端控制器（UserController.java）：**
```java
@PostMapping("/register")
public R register(@RequestBody User user) {
    R res = new R();
    try {
        Integer userId = userService.addUser(user);
        res.setCode(0);
        res.setMsg("注册成功");
        res.setData(userId);
    } catch (Exception e) {
        res.setCode(1);
        res.setMsg("注册失败：" + e.getMessage());
    }
    return res;
}
```

**后端服务层（UserServiceImpl.java）：**
```java
@Override
public Integer addUser(User user) {
    user.setCreditScore(100); // 新用户默认100信用分
    user.setIsadmin(0);
    return userMapper.insertSelective(user);
}
```

**流程说明：**
1. 用户在注册页面填写信息并提交
2. 前端调用 `register` API 发送POST请求到 `/user/register`
3. 后端 `UserController.register()` 接收请求
4. 调用 `UserService.addUser()` 设置默认信用分100，普通用户角色
5. 通过 `UserMapper` 插入数据库
6. 返回注册成功响应给前端
7. 前端提示成功并切换到登录页面

#### 12.2.2 用户登录流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-frontend/src/views/login/index.vue
2. 【完整文件相对路径】代码/book-frontend/src/api/user.js
3. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/UserController.java
4. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/service/impl/UserServiceImpl.java
5. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/utils/TokenProcessor.java
6. 【完整文件相对路径】代码/book-frontend/src/store/modules/user.js
7. 【完整文件相对路径】代码/book-frontend/src/utils/auth.js

**完整代码（关键部分）：**

**后端控制器（UserController.java）：**
```java
@PostMapping("/login")
public R login(@RequestBody User user, HttpSession session) {
    User loginUser = userService.login(user);
    if (loginUser != null) {
        // 生成Token
        String token = TokenProcessor.getInstance().makeToken();
        loginUser.setToken(token);
        // 更新用户Token
        userService.updateUser(loginUser);
        session.setAttribute("userObj", loginUser);
        return R.ok().data("user", loginUser);
    } else {
        return R.error().msg("用户名或密码错误");
    }
}
```

**后端服务层（UserServiceImpl.java）：**
```java
@Override
public User login(User user) {
    return userMapper.login(user.getUsername(), user.getPassword());
}
```

**Token工具类（TokenProcessor.java）：**
```java
public class TokenProcessor {
    private static TokenProcessor instance = new TokenProcessor();
    
    private TokenProcessor() {}
    
    public static TokenProcessor getInstance() {
        return instance;
    }
    
    public String makeToken() {
        String token = (System.currentTimeMillis() + new Random().nextInt(999999999)) + "";
        try {
            MessageDigest md = MessageDigest.getInstance("md5");
            byte[] md5 = md.digest(token.getBytes());
            BASE64Encoder encoder = new BASE64Encoder();
            return encoder.encode(md5);
        } catch (NoSuchAlgorithmException e) {
            throw new RuntimeException(e);
        }
    }
}
```

**前端登录处理（store/modules/user.js）：**
```javascript
import { login } from '@/api/user'
import { setToken, removeToken } from '@/utils/auth'

const actions = {
  login({ commit }, userInfo) {
    const { username, password } = userInfo
    return new Promise((resolve, reject) => {
      login({ username: username.trim(), password: password }).then(response => {
        const { data } = response
        commit('SET_TOKEN', data.token)
        commit('SET_USERID', data.userid)
        commit('SET_NAME', data.username)
        commit('SET_ISADMIN', data.isadmin)
        setToken(data.token)
        resolve()
      }).catch(error => {
        reject(error)
      })
    })
  }
}
```

**流程说明：**
1. 用户在登录页面输入用户名和密码
2. 前端调用 `login` action
3. 发送POST请求到 `/user/login`
4. 后端 `UserController.login()` 接收请求
5. 调用 `UserService.login()` 验证用户名密码
6. 验证成功后，使用 `TokenProcessor` 生成Token
7. 更新用户Token到数据库
8. 将用户信息存入Session
9. 返回用户信息（含Token）给前端
10. 前端将Token存入Vuex和localStorage
11. 登录成功，跳转到首页

### 12.3 图书发布与审核流程

#### 12.3.1 图书发布流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-frontend/src/views/bookinfo/index.vue
2. 【完整文件相对路径】代码/book-frontend/src/api/bookinfo.js
3. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/BookInfoController.java
4. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/service/impl/BookInfoServiceImpl.java
5. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/mapper/BookInfoMapper.java
6. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/uploadController.java

**完整代码（关键部分）：**

**文件上传控制器（uploadController.java）：**
```java
@RequestMapping("/uploadImg")
@ResponseBody
public Map<String, Object> uploadImg(HttpServletRequest req) {
    String resPath = upload(req);
    Map<String, Object> res = new HashMap<>();
    res.put("code", 0);
    res.put("data", resPath);
    return res;
}
```

**后端图书控制器（BookInfoController.java）：**
```java
@PostMapping("/addBookInfo")
public Integer addBookInfo(@RequestBody BookInfo bookInfo) {
    bookInfo.setAuditStatus(0); // 新发布图书待审核
    return bookInfoService.addBookInfo(bookInfo);
}
```

**后端服务层（BookInfoServiceImpl.java）：**
```java
@Override
public Integer addBookInfo(BookInfo bookInfo) {
    if (bookInfo.getInventory() == null) {
        bookInfo.setInventory(bookInfo.getBookcount());
    }
    if (bookInfo.getAuditStatus() == null) {
        bookInfo.setAuditStatus(1);
    }
    return bookInfoMapper.insertSelective(bookInfo);
}
```

**流程说明：**
1. 用户在图书发布页面填写图书信息
2. 首先上传图书封面图片
3. 调用 `/upload/uploadImg` 接口
4. `uploadController` 保存图片到服务器，返回图片访问路径
5. 用户填写完整信息后提交图书
6. 前端调用 `addBookInfo` API
7. 发送POST请求到 `/bookInfo/addBookInfo`
8. 后端设置 `auditStatus=0`（待审核状态）
9. 调用 `BookInfoService.addBookInfo()` 处理
10. 通过 `BookInfoMapper` 插入数据库
11. 返回成功响应
12. 图书进入待审核状态

#### 12.3.2 图书审核流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-frontend/src/views/bookaudit/index.vue
2. 【完整文件相对路径】代码/book-frontend/src/api/bookinfo.js
3. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/BookInfoController.java
4. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/service/impl/BookInfoServiceImpl.java

**完整代码（关键部分）：**

**后端控制器（BookInfoController.java）：**
```java
@PutMapping("/updateBookInfo")
public Integer updateBookInfo(@RequestBody BookInfo bookInfo) {
    return bookInfoService.updateBookInfo(bookInfo);
}
```

**流程说明：**
1. 管理员登录系统，进入图书审核页面
2. 前端调用 `queryBookInfosByPage` 获取待审核图书列表（auditStatus=0）
3. 管理员查看图书详情，决定通过或驳回
4. 点击审核通过时，设置 `auditStatus=1`
5. 点击驳回时，设置 `auditStatus=2`
6. 调用 `updateBookInfo` API 更新图书状态
7. 后端 `BookInfoController.updateBookInfo()` 接收请求
8. 调用 `BookInfoService.updateBookInfo()` 更新数据库
9. 审核完成，图书状态更新

### 12.4 借阅申请与审批流程

#### 12.4.1 借阅申请流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-frontend/src/views/bookinfo/index.vue
2. 【完整文件相对路径】代码/book-frontend/src/api/borrow.js
3. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/BorrowController.java
4. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/service/impl/BorrowServiceImpl.java
5. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/mapper/BorrowMapper.java
6. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/utils/WechatPushUtils.java

**完整代码（关键部分）：**

**后端控制器（BorrowController.java）：**
```java
@PostMapping("/addBorrow")
public R addBorrow(@RequestBody Borrow borrow) {
    try {
        borrowService.addBorrow(borrow);
        return R.ok().msg("申请成功");
    } catch (Exception e) {
        return R.error().msg(e.getMessage());
    }
}
```

**后端服务层（BorrowServiceImpl.java）：**
```java
@Override
@Transactional
public void addBorrow(Borrow borrow) {
    // 检查信用分
    User user = userMapper.selectByPrimaryKey(borrow.getUserId());
    if (user.getCreditScore() < 60) {
        throw new OperationFailureException("信用分不足60分，无法申请借阅");
    }
    
    // 检查图书库存
    BookInfo book = bookInfoMapper.selectByPrimaryKey(borrow.getBookId());
    if (book.getInventory() <= 0) {
        throw new BookNotEnoughException("图书库存不足");
    }
    
    // 生成交接暗号
    String verifyCode = generateVerifyCode();
    borrow.setVerifyCode(verifyCode);
    borrow.setStatus(0); // 待审核
    borrow.setApplyTime(new Date());
    
    borrowMapper.insertSelective(borrow);
    
    // 微信推送通知图书所有者
    WechatPushUtils.sendBorrowApplyNotification(book.getOwnerId(), borrow);
}

private String generateVerifyCode() {
    Random random = new Random();
    return String.format("%06d", random.nextInt(1000000));
}
```

**微信推送工具（WechatPushUtils.java）：**
```java
public class WechatPushUtils {
    public static void sendBorrowApplyNotification(Integer ownerId, Borrow borrow) {
        User owner = userMapper.selectByPrimaryKey(ownerId);
        if (owner != null && owner.getWxOpenId() != null) {
            // 调用WxPusher发送微信通知
            String content = "您的图书《" + borrow.getBookName() + "》有新的借阅申请，请及时处理！";
            sendWxPusherMessage(owner.getWxOpenId(), content);
        }
    }
}
```

**流程说明：**
1. 用户浏览图书，点击借阅按钮
2. 填写借阅天数等信息，提交申请
3. 前端调用 `addBorrow` API
4. 发送POST请求到 `/borrow/addBorrow`
5. 后端 `BorrowController.addBorrow()` 接收请求
6. 调用 `BorrowService.addBorrow()` 处理业务逻辑：
   - 检查申请人信用分是否≥60
   - 检查图书库存是否充足
   - 生成6位数字交接暗号
   - 设置借阅状态为0（待审核）
   - 记录申请时间
7. 通过 `BorrowMapper` 插入借阅记录
8. 调用 `WechatPushUtils` 给图书所有者发送微信推送
9. 返回申请成功响应
10. 前端提示成功

#### 12.4.2 借阅审批流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-frontend/src/views/borrow/index.vue
2. 【完整文件相对路径】代码/book-frontend/src/api/borrow.js
3. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/BorrowController.java
4. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/service/impl/BorrowServiceImpl.java

**完整代码（关键部分）：**

**后端控制器（BorrowController.java）：**
```java
@PutMapping("/audit")
public R auditBorrow(@RequestBody Borrow borrow) {
    try {
        borrowService.auditBorrow(borrow);
        return R.ok().msg("操作成功");
    } catch (Exception e) {
        return R.error().msg(e.getMessage());
    }
}
```

**后端服务层（BorrowServiceImpl.java）：**
```java
@Override
@Transactional
public void auditBorrow(Borrow borrow) {
    Borrow existingBorrow = borrowMapper.selectByPrimaryKey(borrow.getBorrowId());
    
    if (borrow.getStatus() == 1) { // 审核通过
        // 扣减库存
        BookInfo book = bookInfoMapper.selectByPrimaryKey(existingBorrow.getBookId());
        book.setInventory(book.getInventory() - 1);
        bookInfoMapper.updateByPrimaryKeySelective(book);
        
        existingBorrow.setStatus(1);
        existingBorrow.setAuditTime(new Date());
    } else if (borrow.getStatus() == 2) { // 审核拒绝
        existingBorrow.setStatus(2);
        existingBorrow.setAuditTime(new Date());
    }
    
    borrowMapper.updateByPrimaryKeySelective(existingBorrow);
}
```

**流程说明：**
1. 图书所有者（或管理员）查看借阅申请列表
2. 查看申请人信息和借阅详情
3. 选择通过或拒绝申请
4. 点击通过时，设置 `status=1`
5. 点击拒绝时，设置 `status=2`
6. 调用 `auditBorrow` API
7. 后端 `BorrowService.auditBorrow()` 处理：
   - 如果通过：扣减图书库存1本，更新借阅状态为1，记录审核时间
   - 如果拒绝：更新借阅状态为2，记录审核时间
8. 更新数据库
9. 返回成功响应

### 12.5 图书交接与归还流程

#### 12.5.1 图书交接流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-frontend/src/views/borrow/index.vue
2. 【完整文件相对路径】代码/book-frontend/src/api/borrow.js
3. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/BorrowController.java
4. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/service/impl/BorrowServiceImpl.java

**完整代码（关键部分）：**

**后端服务层（BorrowServiceImpl.java）：**
```java
@Override
@Transactional
public void handover(Integer borrowId, String verifyCode) {
    Borrow borrow = borrowMapper.selectByPrimaryKey(borrowId);
    
    if (!borrow.getVerifyCode().equals(verifyCode)) {
        throw new OperationFailureException("交接暗号错误");
    }
    
    borrow.setStatus(3); // 已交接/借阅中
    borrow.setHandoverTime(new Date());
    
    // 计算应还时间
    Calendar calendar = Calendar.getInstance();
    calendar.setTime(borrow.getHandoverTime());
    calendar.add(Calendar.DAY_OF_MONTH, borrow.getBorrowDays());
    borrow.setDueTime(calendar.getTime());
    
    borrowMapper.updateByPrimaryKeySelective(borrow);
}
```

**流程说明：**
1. 借阅双方约定时间地点进行线下交接
2. 图书所有者告知借阅人交接暗号
3. 借阅人在系统中输入交接暗号
4. 前端调用交接接口，传递 `borrowId` 和 `verifyCode`
5. 后端 `BorrowService.handover()` 验证暗号
6. 验证成功后：
   - 更新借阅状态为3（借阅中）
   - 记录交接时间
   - 根据借阅天数计算应还时间
7. 更新数据库
8. 交接完成，图书进入借阅状态

#### 12.5.2 图书归还流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-frontend/src/views/borrow/index.vue
2. 【完整文件相对路径】代码/book-frontend/src/api/borrow.js
3. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/BorrowController.java
4. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/service/impl/BorrowServiceImpl.java

**完整代码（关键部分）：**

**后端服务层（BorrowServiceImpl.java）：**
```java
@Override
@Transactional
public void returnBook(Integer borrowId, Integer bookCondition) {
    Borrow borrow = borrowMapper.selectByPrimaryKey(borrowId);
    
    borrow.setStatus(4); // 已归还
    borrow.setReturnTime(new Date());
    borrow.setBookCondition(bookCondition);
    
    // 增加库存
    BookInfo book = bookInfoMapper.selectByPrimaryKey(borrow.getBookId());
    book.setInventory(book.getInventory() + 1);
    bookInfoMapper.updateByPrimaryKeySelective(book);
    
    // 检查是否逾期
    User user = userMapper.selectByPrimaryKey(borrow.getUserId());
    if (borrow.getReturnTime().after(borrow.getDueTime())) {
        // 逾期，扣信用分
        int daysOverdue = (int) ((borrow.getReturnTime().getTime() - borrow.getDueTime().getTime()) / (1000 * 60 * 60 * 24));
        int creditDeduction = Math.min(daysOverdue * 5, 50); // 每天扣5分，最多扣50分
        user.setCreditScore(Math.max(0, user.getCreditScore() - creditDeduction));
    }
    
    // 检查图书状况
    if (bookCondition == 2) { // 损坏
        user.setCreditScore(Math.max(0, user.getCreditScore() - 20)); // 扣20分
    }
    
    userMapper.updateByPrimaryKeySelective(user);
    borrowMapper.updateByPrimaryKeySelective(borrow);
}
```

**流程说明：**
1. 借阅人阅读完毕，联系图书所有者约定归还
2. 双方线下进行图书归还交接
3. 图书所有者检查图书状况（完好/轻微损坏/严重损坏）
4. 在系统中确认归还，选择图书状况
5. 前端调用归还接口
6. 后端 `BorrowService.returnBook()` 处理：
   - 更新借阅状态为4（已归还）
   - 记录归还时间
   - 记录图书状况
   - 增加图书库存1本
   - 检查是否逾期：逾期按天扣信用分（每天5分，最多50分）
   - 检查图书状况：损坏扣20分信用分
   - 更新用户信用分
7. 更新数据库
8. 归还完成

### 12.6 定时任务处理流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/task/BorrowTask.java
2. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/BackendApplication.java

**完整代码：**

**主启动类（BackendApplication.java）：**
```java
@SpringBootApplication
@EnableScheduling
@EnableTransactionManagement
public class BackendApplication {
    public static void main(String[] args) {
        SpringApplication.run(BackendApplication.class, args);
    }
}
```

**定时任务（BorrowTask.java）：**
```java
@Component
@Slf4j
public class BorrowTask {

    @Resource
    private BorrowMapper borrowMapper;
    
    @Resource
    private UserMapper userMapper;

    /**
     * 每天凌晨2点执行：自动取消超时未审核的借阅申请
     */
    @Scheduled(cron = "0 0 2 * * ?")
    public void cancelOverdueApply() {
        log.info("开始执行定时任务：取消超时未审核的借阅申请");
        
        List<Borrow> borrows = borrowMapper.selectTimeoutApplies();
        for (Borrow borrow : borrows) {
            borrow.setStatus(5); // 已取消
            borrowMapper.updateByPrimaryKeySelective(borrow);
            log.info("已取消超时借阅申请：borrowId={}", borrow.getBorrowId());
        }
        
        log.info("定时任务完成：共取消{}条超时申请", borrows.size());
    }

    /**
     * 每天凌晨3点执行：检查逾期未还图书，自动扣信用分
     */
    @Scheduled(cron = "0 0 3 * * ?")
    public void checkOverdueBooks() {
        log.info("开始执行定时任务：检查逾期未还图书");
        
        List<Borrow> overdueBorrows = borrowMapper.selectOverdueBorrows();
        int count = 0;
        
        for (Borrow borrow : overdueBorrows) {
            User user = userMapper.selectByPrimaryKey(borrow.getUserId());
            if (user != null && user.getCreditScore() > 0) {
                // 每天扣2分
                user.setCreditScore(Math.max(0, user.getCreditScore() - 2));
                userMapper.updateByPrimaryKeySelective(user);
                count++;
                log.info("用户{}逾期未还，扣除信用分2分", user.getUsername());
            }
        }
        
        log.info("定时任务完成：共处理{}条逾期记录", count);
    }
}
```

**流程说明：**
1. 系统启动时，`BackendApplication` 的 `@EnableScheduling` 注解启用定时任务
2. **每天凌晨2点** 执行 `cancelOverdueApply()`：
   - 查询超过3天未审核的借阅申请
   - 将状态更新为5（已取消）
   - 记录日志
3. **每天凌晨3点** 执行 `checkOverdueBooks()`：
   - 查询所有逾期未还的借阅记录
   - 对每个逾期用户扣除2分信用分
   - 记录日志

### 12.7 心愿书单发布与认领流程

**涉及文件：**
1. 【完整文件相对路径】代码/book-frontend/src/views/wish/index.vue
2. 【完整文件相对路径】代码/book-frontend/src/api/wish.js
3. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/web/BookWishController.java
4. 【完整文件相对路径】代码/book-backend/src/main/java/com/shanzhu/book/mapper/BookWishMapper.java

**流程说明：**
1. 用户有想要的图书，发布心愿书单
2. 填写书名、描述等信息提交
3. 前端调用 `addWish` API
4. 后端 `BookWishController.addWish()` 接收请求
5. 调用 `BookWishMapper.insertWish()` 插入数据库，状态设为0（未完成）
6. 其他用户浏览心愿书单
7. 如果有用户有这本书，可以点击认领
8. 前端调用 `fulfillWish` API
9. 后端更新心愿状态为1（已完成），记录认领用户ID
10. 心愿完成

### 12.8 完整数据流转总结图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         校园旧书漂流共享系统 - 数据流转总览                    │
└─────────────────────────────────────────────────────────────────────────┘

用户操作层
├─ 普通用户
│  ├─ 注册/登录 → 用户认证模块 → 生成Token → 存入Session/Vuex
│  ├─ 浏览图书 → 图书管理模块 → 查询数据库 → 返回图书列表
│  ├─ 发布图书 → 上传图片 → 文件上传模块 → 保存到服务器
│  │           → 提交信息 → 图书管理模块 → 存入数据库(待审核)
│  ├─ 申请借阅 → 借阅管理模块 → 检查信用/库存 → 生成借阅记录
│  │           → 微信推送模块 → 通知图书所有者
│  ├─ 确认交接 → 借阅管理模块 → 验证暗号 → 更新状态(借阅中)
│  ├─ 发布心愿 → 心愿书单模块 → 存入数据库
│  └─ 查看消息 → 消息通知模块 → 查询用户消息
│
└─ 管理员
   ├─ 审核图书 → 图书管理模块 → 更新auditStatus
   ├─ 审核借阅 → 借阅管理模块 → 扣减库存/拒绝申请
   ├─ 管理用户 → 用户管理模块 → 查询/编辑用户
   └─ 管理类型 → 图书类型模块 → 增删改查图书分类

后端处理层
├─ Controller层
│  ├─ UserController → 处理用户相关请求
│  ├─ BookInfoController → 处理图书相关请求
│  ├─ BorrowController → 处理借阅相关请求
│  ├─ BookTypeController → 处理图书类型请求
│  ├─ BookWishController → 处理心愿书单请求
│  ├─ MessageController → 处理消息通知请求
│  └─ uploadController → 处理文件上传请求
│
├─ Service层
│  ├─ UserService → 用户业务逻辑
│  ├─ BookInfoService → 图书业务逻辑
│  ├─ BorrowService → 借阅业务逻辑
│  └─ BookTypeService → 图书类型业务逻辑
│
├─ Mapper层
│  ├─ UserMapper → 用户数据访问
│  ├─ BookInfoMapper → 图书数据访问
│  ├─ BorrowMapper → 借阅数据访问
│  ├─ BookTypeMapper → 图书类型数据访问
│  ├─ BookWishMapper → 心愿书单数据访问
│  └─ MessageMapper → 消息数据访问
│
├─ 定时任务
│  └─ BorrowTask → 自动取消超时申请、逾期扣分
│
└─ 工具类
   ├─ TokenProcessor → Token生成
   ├─ WechatPushUtils → 微信推送
   ├─ PageUtils → 分页处理
   ├─ PathUtils → 路径处理
   ├─ JsonUtil → JSON处理
   └─ R → 统一响应

数据存储层
└─ MySQL数据库
   ├─ user表 → 用户信息
   ├─ book_info表 → 图书信息
   ├─ borrow表 → 借阅记录
   ├─ book_type表 → 图书类型
   ├─ book_wish表 → 心愿书单
   └─ message表 → 消息通知
```

---

## 总结

本文档完整分析了校园旧书漂流共享系统的所有功能模块，包括：

**上篇已包含：**
1. 项目概览与技术栈
2. 项目结构解析
3. 用户认证与管理模块
4. 图书信息管理模块
5. 借阅流程管理模块

**下篇包含：**
6. 图书类型管理模块
7. 心愿书单管理模块
8. 消息通知模块
9. 文件上传模块
10. 系统配置与异常处理模块
11. 工具类模块
12. 数据看板模块
13. 项目功能联动与数据流转说明

每个模块都包含了：
- 模块概述
- 所有涉及的代码文件（包含完整的相对路径）
- 完整的代码内容
- 关键业务逻辑说明
- API接口说明

最后，详细阐述了整个项目的功能联动与数据流转，通过具体的业务场景（注册登录、图书发布审核、借阅申请审批、交接归还、定时任务等），清晰地展示了各个模块之间的协作关系和数据流向。
