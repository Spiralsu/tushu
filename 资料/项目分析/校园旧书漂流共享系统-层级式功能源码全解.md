# 校园旧书漂流共享系统 - 层级式功能源码全解

> 本文档严格按照树形层级结构展开，每个最细粒度功能点均附带完整代码实现。

---

## 一、用户模块

```
用户模块 
 ├── 认证功能 
 │   ├── 用户登录 
 │   │   ├── 学号密码验证 
 │   │   ├── 身份校验（读者/管理员） 
 │   │   ├── 状态校验（待审核/正常/禁用） 
 │   │   ├── Token生成与存储 
 │   │   └── 微信推送绑定检查 
 │   ├── 用户注册 
 │   │   ├── 学号唯一性检查 
 │   │   ├── 信息录入（姓名、学号、密码、性别、系部） 
 │   │   ├── 默认状态设为"待审核" 
 │   │   └── 初始信用分100分 
 │   └── 退出登录 
 │       ├── 清除本地Token 
 │       ├── 清除Vuex状态 
 │       └── 清除服务端Token映射 
 ├── 个人信息管理 
 │   ├── 个人信息查看 
 │   │   ├── 查看基本信息（姓名、学号、性别、系部） 
 │   │   ├── 查看信用分 
 │   │   ├── 查看今日已加分 
 │   │   └── 查看微信绑定状态 
 │   ├── 修改密码 
 │   │   ├── 原密码验证 
 │   │   ├── 新密码设置 
 │   │   └── 确认新密码 
 │   └── 微信推送绑定 
 │       ├── 输入WxPusher UID 
 │       ├── 保存到数据库open_id字段 
 │       └── 解绑功能 
 └── 信用分系统 
     ├── 信用分查看 
     │   ├── 显示当前信用分 
     │   ├── 显示今日已恢复分数 
     │   └── 显示风控状态（≥60正常，<60限制借阅） 
     ├── 信用分奖励 
     │   ├── 发布图书奖励 +2分（每日上限5分） 
     │   ├── 成功交接奖励 +3分（每日上限5分） 
     │   ├── 当日额度控制（每日最多恢复5分） 
     │   └── 封顶100分 
     └── 信用分惩罚 
         ├── 逾期每日扣 -2分 
         ├── 报损登记扣 -10分 
         ├── 强制收回扣 -10分 
         └── 最低0分
```

---

### 1.1 认证功能

#### 1.1.1 用户登录

##### 学号密码验证

**后端代码 - UserController.java**
```java
// 登录接口
@PostMapping(value = "/login")
public R login(@RequestBody User user) {
    // 兼容处理：前端若传username，赋值给studentid
    if(user.getStudentid() == null) user.setStudentid(user.getUsername());
    return userService.userLogin(user);
}
```

**后端代码 - UserServiceImpl.java**
```java
@Override
public R userLogin(User user) {
    // 使用学号(studentId)登录
    User dbUser = userMapper.selectByUserName(user.getStudentid());
    if (dbUser == null) return R.error("学号未注册");

    // 1. 校验密码
    if (!dbUser.getUserpassword().equals(user.getUserpassword())) {
        return R.error("密码错误");
    }
    // ... 后续校验
}
```

**前端代码 - login/index.vue**
```vue
<el-form ref="loginForm" :model="loginForm" :rules="loginRules" class="login-form">
  <el-form-item prop="username">
    <el-input
      ref="username"
      v-model="loginForm.username"
      placeholder="请输入学号/工号"
      name="username"
      type="text"
      tabindex="1"
      prefix-icon="el-icon-user"
    />
  </el-form-item>

  <el-form-item prop="password">
    <el-input
      :key="passwordType"
      ref="password"
      v-model="loginForm.password"
      :type="passwordType"
      placeholder="请输入密码"
      name="password"
      tabindex="2"
      prefix-icon="el-icon-lock"
      @keyup.enter.native="handleLogin"
    />
  </el-form-item>
</el-form>
```

```javascript
handleLogin() {
  this.$refs.loginForm.validate(valid => {
    if (valid) {
      this.loading = true
      this.$store.dispatch('user/login', this.loginForm).then(() => {
        const targetPath = this.redirect || '/';
        if (targetPath.includes('/404')) {
          this.$router.push({ path: '/' });
        } else {
          this.$router.push({ path: targetPath });
        }
        this.loading = false
      }).catch(() => {
        this.loading = false
      })
    }
  })
}
```

---

##### 身份校验（读者/管理员）

**后端代码 - UserServiceImpl.java**
```java
// 2. 校验身份 (0:读者, 1:管理员)
if (user.getIsadmin() != null && !user.getIsadmin().equals(dbUser.getIsadmin())) {
    String roleName = user.getIsadmin() == 1 ? "管理员" : "读者";
    return R.error("登录失败：该账号不是" + roleName + "身份");
}
```

---

##### 状态校验（待审核/正常/禁用）

**后端代码 - UserServiceImpl.java**
```java
// 3. 校验状态
if (dbUser.getStatus() == 0) return R.error("账号审核中，请联系管理员");
if (dbUser.getStatus() == 2) return R.error("账号被禁用");
```

**实体类 - User.java**
```java
@Data
public class User implements Serializable {
    private Integer userid;
    private String username;
    private String studentid;     // 学号
    private String userpassword;
    private Integer isadmin;      // 0:读者 1:管理员
    private Integer status;       // 0:待审核 1:正常 2:禁用
    private Integer gender;       // 性别
    private String openId;        // 微信测试号的OpenID
    private String department;    // 所属系部
    private Integer creditScore;  // 信用分
    private Integer todayAddScore;     // 今日已加分
    private java.util.Date scoreUpdateDate;  // 积分更新日期
}
```

---

##### Token生成与存储

**后端代码 - UserServiceImpl.java**
```java
String token = TokenProcessor.getInstance().generateToken();
saveUser(token, dbUser);

return R.ok("登录成功").put("token", token).put("user", dbUser);
```

```java
@Override
public void saveUser(String token, User user) { 
    TokenProcessor.getInstance().saveUser(token, user); 
}
@Override
public User getUser(String token) { 
    return TokenProcessor.getInstance().getUser(token); 
}
```

---

##### 微信推送绑定检查

**前端代码 - dashboard/index.vue**
```javascript
openProfileDialog() {
  this.profileDialogVisible = true;
  queryUsersByPage({ username: this.username, page: 1, limit: 1 }).then(res => {
    const list = res.data || res.rows || res;
    if (list && list.length > 0) {
      this.myCreditScore = list[0].creditScore != null ? list[0].creditScore : 100;
      // 判定是否有绑定的微信 openid
      this.isWxBound = !!(list[0].openid || list[0].openId);
    }
  }).catch(() => {});
}
```

---

#### 1.1.2 用户注册

##### 学号唯一性检查

**后端代码 - UserServiceImpl.java**
```java
@Override
public R userRegister(User user) {
    // 查重：检查学号
    if (userMapper.selectCountByUserName(user.getStudentid()) > 0) {
        return R.error("该学号已注册");
    }
    user.setStatus(0); // 默认待审核
    user.setIsadmin(0);
    int i = userMapper.insert(user);
    return i > 0 ? R.ok("注册成功，请等待审核") : R.error("注册失败");
}
```

---

##### 信息录入（姓名、学号、密码、性别、系部）

**前端代码 - user/index.vue（管理员录入用户）**
```vue
<el-form ref="dataForm" :rules="rules" :model="temp" label-position="left" label-width="80px">
  <el-form-item label="姓名" prop="username">
    <el-input v-model="temp.username" placeholder="请输入真实姓名" />
  </el-form-item>
  <el-form-item label="学号" prop="studentid">
    <el-input v-model="temp.studentid" placeholder="作为登录账号" :disabled="dialogStatus==='update'" />
  </el-form-item>
  <el-form-item label="系部" prop="department">
    <el-select v-model="temp.department" placeholder="请选择所属系部" style="width: 100%;" clearable>
      <el-option v-for="dept in departments" :key="dept" :label="dept" :value="dept" />
    </el-select>
  </el-form-item>
  <el-form-item label="性别" prop="gender">
    <el-radio-group v-model="temp.gender">
      <el-radio :label="1">男生</el-radio>
      <el-radio :label="0">女生</el-radio>
      <el-radio :label="2">保密</el-radio>
    </el-radio-group>
  </el-form-item>
</el-form>
```

---

##### 默认状态设为"待审核"

**后端代码 - UserServiceImpl.java**
```java
user.setStatus(0); // 默认待审核
user.setIsadmin(0);
int i = userMapper.insert(user);
```

---

##### 初始信用分100分

**后端代码 - UserServiceImpl.java**
```java
@Override
public Integer addUser(User user) { 
    // 默认设置
    if(user.getStatus() == null) user.setStatus(1); // 正常状态
    if(user.getIsadmin() == null) user.setIsadmin(0); // 默认读者
    return userMapper.insert(user); 
}
```

**前端代码 - user/index.vue**
```javascript
data() {
  return {
    temp: { 
      userid: undefined, 
      username: '', 
      studentid: '', 
      isadmin: 0, 
      status: 1, 
      userpassword: '', 
      gender: 2, 
      department: '', 
      creditScore: 100  // 默认信用分100分
    }
  }
}
```

---

#### 1.1.3 退出登录

##### 清除本地Token

**前端代码 - Vuex store/user.js**
```javascript
logout({ commit, state, dispatch }) {
  return new Promise((resolve, reject) => {
    logout(state.token).then(() => {
      commit('SET_TOKEN', '')
      commit('SET_ROLES', [])
      removeToken()
      resetRouter()
      commit('RESET_STATE')
      resolve()
    }).catch(error => {
      reject(error)
    })
  })
}
```

---

##### 清除Vuex状态

**前端代码 - Vuex store**
```javascript
commit('SET_TOKEN', '')
commit('SET_ROLES', [])
commit('RESET_STATE')
```

---

##### 清除服务端Token映射

**后端代码 - UserController.java**
```java
// 退出登录
@RequestMapping(value = "/logout")
public R logout(String token) {
    userService.removeUser(token);
    return R.ok("退出成功");
}
```

**后端代码 - UserServiceImpl.java**
```java
@Override
public void removeUser(String token) { 
    TokenProcessor.getInstance().removeUser(token); 
}
```

---

### 1.2 个人信息管理

#### 1.2.1 个人信息查看

##### 查看基本信息（姓名、学号、性别、系部）

**前端代码 - dashboard/index.vue**
```vue
<el-dialog title="个人中心" :visible.sync="profileDialogVisible" width="400px" center>
  <div class="profile-card">
    <div class="avatar-wrapper"><i class="el-icon-user-solid"></i></div>
    <h3 class="username">书友 {{ name || '未知用户' }}</h3>
    <p class="role-tag">
      <el-tag size="small" :type="roles[0] === 'admin' ? 'danger' : 'success'">
        {{ roles[0] === 'admin' ? '系统管理员' : '漂流书友' }}
      </el-tag>
    </p>
    <div class="profile-details">
      <div class="detail-item">
        <span class="label">学号 / 账号：</span>
        <span class="value" style="color: #409eff;">{{ username }}</span>
      </div>
    </div>
  </div>
</el-dialog>
```

---

##### 查看信用分

**前端代码 - dashboard/index.vue**
```vue
<div class="detail-item" style="margin-top: 15px; border-top: 1px dashed #dcdfe6; padding-top: 15px; align-items: center;">
  <span class="label">当前信用分：</span>
  <span class="value">
    <el-tag :type="myCreditScore >= 90 ? 'success' : (myCreditScore >= 60 ? 'warning' : 'danger')" 
            effect="dark" style="font-size: 16px; border-radius: 10px; cursor: pointer;">
      <i class="el-icon-s-data"></i> {{ myCreditScore }} 分 <i class="el-icon-question" style="font-size:14px;"></i>
    </el-tag>
  </span>
</div>
```

```javascript
openProfileDialog() {
  this.profileDialogVisible = true;
  queryUsersByPage({ username: this.username, page: 1, limit: 1 }).then(res => {
    const list = res.data || res.rows || res;
    if (list && list.length > 0) {
      this.myCreditScore = list[0].creditScore != null ? list[0].creditScore : 100;
    }
  }).catch(() => {});
}
```

---

##### 查看今日已加分

**实体类 - User.java**
```java
// 每日回血上限风控字段
private Integer todayAddScore;
private java.util.Date scoreUpdateDate;
```

---

##### 查看微信绑定状态

**前端代码 - dashboard/index.vue**
```vue
<el-dialog title="📲 微信实时推送管理" :visible.sync="wxDialogVisible" width="500px">
  <div class="wx-dialog-body">
    <div class="status-header">
      <i class="el-icon-chat-dot-round wx-icon" :class="{'is-bound': isWxBound}"></i>
      <div class="status-text">
        当前状态：
        <el-tag :type="isWxBound ? 'success' : 'info'" size="small" effect="dark">
          <i :class="isWxBound ? 'el-icon-success' : 'el-icon-warning'"></i>
          {{ isWxBound ? '已绑定' : '未绑定' }}
        </el-tag>
      </div>
    </div>
  </div>
</el-dialog>
```

---

#### 1.2.2 修改密码

##### 原密码验证

**后端代码 - UserServiceImpl.java**
```java
@Override
public void setPassword(Integer id, String password) {
    User user = new User();
    user.setUserid(id);
    user.setUserpassword(password);
    userMapper.updateByPrimaryKeySelective(user);
}
```

---

##### 新密码设置

**前端代码 - 密码修改页面**
```javascript
goToPassword() { 
  this.profileDialogVisible = false; 
  this.$router.push('/other/password'); 
}
```

---

##### 确认新密码

**前端代码 - user/index.vue（管理员重置密码）**
```javascript
handleResetPwd(row) {
  this.$confirm(`确定要重置 ${row.username} 的密码为 123456 吗？`, '重置密码', { type: 'warning' }).then(() => {
    updateUser({ userid: row.userid, userpassword: '123456' }).then(() => { 
      this.$message.success('密码已重置') 
    })
  })
}
```

---

#### 1.2.3 微信推送绑定

##### 输入WxPusher UID

**前端代码 - dashboard/index.vue**
```vue
<el-input v-model="wxOpenId" placeholder="请粘贴您的专属 UID (格式如: UID_xXyY...)" class="uid-input" clearable>
  <template slot="prepend"><i class="el-icon-key"></i></template>
</el-input>
```

```javascript
openWxPusherWindow() {
  const url = 'https://wxpusher.zjiecode.com/wxuser/?type=1&id=120258#/follow';
  const width = 400;
  const height = 650;
  const left = (window.screen.width - width) / 2;
  const top = (window.screen.height - height) / 2;
  window.open(url, 'WxPusher', `width=${width},height=${height},top=${top},left=${left}`);
}
```

---

##### 保存到数据库open_id字段

**前端代码 - dashboard/index.vue**
```javascript
submitBindWx() {
  if(!this.wxOpenId || !this.wxOpenId.startsWith('UID_')) 
    return this.$message.warning("格式不正确！请填入以 UID_ 开头的完整字符。");
  request({ 
    url: '/borrow/bindWx', 
    method: 'post', 
    params: { userId: this.id, openId: this.wxOpenId } 
  }).then(res => {
    if(res.code === 0 || res === 1) {
      this.$message.success("微信推送绑定成功！");
      this.isWxBound = true;
      this.wxDialogVisible = false;
    } else {
      this.$message.error(res.msg || "绑定失败");
    }
  });
}
```

**后端代码 - BorrowServiceImpl.java**
```java
// 绑定微信 UID
@Override
public R bindWx(Integer userId, String openId) {
    User user = new User();
    user.setUserid(userId);
    user.setOpenId(openId);
    userMapper.updateByPrimaryKeySelective(user);
    return R.ok("微信绑定成功");
}
```

---

##### 解绑功能

**前端代码 - dashboard/index.vue**
```javascript
submitUnbindWx() {
  request({ url: '/borrow/unbindWx', method: 'post', params: { userId: this.id } }).then(res => {
    if(res.code === 0 || res === 1) {
      this.$message.success("微信解绑成功！");
      this.isWxBound = false;
      this.wxOpenId = '';
    }
  });
}
```

**后端代码 - BorrowServiceImpl.java**
```java
// 解除微信绑定
@Override
public R unbindWx(Integer userId) {
    User user = new User();
    user.setUserid(userId);
    user.setOpenId(""); // 清空数据库中的绑定记录
    userMapper.updateByPrimaryKeySelective(user);
    return R.ok("微信解绑成功");
}
```

---

### 1.3 信用分系统

#### 1.3.1 信用分查看

##### 显示当前信用分

**前端代码 - dashboard/index.vue**
```vue
<el-tag :type="myCreditScore >= 90 ? 'success' : (myCreditScore >= 60 ? 'warning' : 'danger')" 
        effect="dark" style="font-size: 16px; border-radius: 10px; cursor: pointer;">
  <i class="el-icon-s-data"></i> {{ myCreditScore }} 分
</el-tag>
```

---

##### 显示今日已恢复分数

**后端代码 - BookInfoServiceImpl.java**
```java
int todayAdded = uploader.getTodayAddScore() == null ? 0 : uploader.getTodayAddScore();
// 如果今天还没加过分，重置今日额度
if (!todayStr.equals(updateStr)) {
    todayAdded = 0;
    uploader.setScoreUpdateDate(new java.util.Date());
}
```

---

##### 显示风控状态（≥60正常，<60限制借阅）

**前端代码 - dashboard/index.vue**
```vue
<div style="font-size: 12px; color: #909399; margin-top: 8px;" v-if="myCreditScore < 100">
  <i class="el-icon-warning-outline"></i> 信用低于 60 分将被限制借阅书籍
</div>
```

**信用分规则弹窗**
```vue
<el-dialog title="📋 信用分流转与惩罚规则" :visible.sync="ruleVisible" width="450px">
  <div style="line-height: 1.8; color: #606266; font-size: 14px;">
    <p><strong>🌟 信用分回血（每日最高可+5分）：</strong><br/>
    1. 发布一本闲置旧书：<span style="color:#67C23A; font-weight:bold;">+2 分</span><br/>
    2. 成功将书交接给下一位书友：<span style="color:#67C23A; font-weight:bold;">+3 分</span></p>
    <p><strong>⚠️ 违规扣分记录：</strong><br/>
    1. 逾期未还：系统每日凌晨巡查，<span style="color:#F56C6C; font-weight:bold;">每日扣除 2 分</span><br/>
    2. 遗失书籍：<span style="color:#F56C6C; font-weight:bold;">单次扣除 10 分</span><br/>
    3. 无视催还被管理员强裁：<span style="color:#F56C6C; font-weight:bold;">强制扣除 10 分</span></p>
    <div style="margin-top: 20px; padding: 12px; background: #fef0f0; border-radius: 8px; color: #F56C6C;">
      <i class="el-icon-warning"></i> <strong>极度警告：</strong><br/>
      当信用分低于 60 分时，系统将彻底锁死您的所有借阅权限！
    </div>
  </div>
</el-dialog>
```

---

#### 1.3.2 信用分奖励

##### 发布图书奖励 +2分（每日上限5分）

**后端代码 - BookInfoServiceImpl.java**
```java
// 发布成功通知与严格【做贡献回血策略】
if (result > 0 && bookInfo.getUploaderid() != null) {
    messageMapper.insert(new Message(bookInfo.getUploaderid(), "【系统通知】您的书籍《" + bookInfo.getBookname() + "》已发布！"));

    User uploader = userMapper.selectByPrimaryKey(bookInfo.getUploaderid());
    if (uploader != null && (uploader.getCreditScore() == null || uploader.getCreditScore() < 100)) {
        java.text.SimpleDateFormat sdf = new java.text.SimpleDateFormat("yyyy-MM-dd");
        String todayStr = sdf.format(new java.util.Date());
        String updateStr = uploader.getScoreUpdateDate() == null ? "" : sdf.format(uploader.getScoreUpdateDate());

        int todayAdded = uploader.getTodayAddScore() == null ? 0 : uploader.getTodayAddScore();
        // 如果今天还没加过分，重置今日额度
        if (!todayStr.equals(updateStr)) {
            todayAdded = 0;
            uploader.setScoreUpdateDate(new java.util.Date());
        }

        int currentScore = uploader.getCreditScore() == null ? 100 : uploader.getCreditScore();
        // 只有今天加分还没满5分，且没满100分时才加
        if (todayAdded < 5 && currentScore < 100) {
            // 发布书籍理论加2分，但不能超过(5-已加分数)的剩余额度
            int canAdd = Math.min(2, 5 - todayAdded);
            int finalScore = Math.min(100, currentScore + canAdd);
            int actualAdded = finalScore - currentScore;

            if (actualAdded > 0) {
                uploader.setCreditScore(finalScore);
                uploader.setTodayAddScore(todayAdded + actualAdded);
                userMapper.updateByPrimaryKeySelective(uploader);
                messageMapper.insert(new Message(uploader.getUserid(), 
                    "【信用奖励】发布书籍奖励生效！本次恢复 " + actualAdded + " 分 (今日已累计恢复 " + (todayAdded + actualAdded) + "/5 分)，当前信用分：" + finalScore + " 分。"));
            }
        }
    }
}
```

---

##### 成功交接奖励 +3分（每日上限5分）

**后端代码 - BorrowServiceImpl.java**
```java
// 【成功交接回血策略】
BookInfo book = bookInfoMapper.selectByPrimaryKey(borrow.getBookid());
if (book != null && book.getUploaderid() != null) {
    User uploader = userMapper.selectByPrimaryKey(book.getUploaderid());
    if (uploader != null && (uploader.getCreditScore() == null || uploader.getCreditScore() < 100)) {
        java.text.SimpleDateFormat sdf = new java.text.SimpleDateFormat("yyyy-MM-dd");
        String todayStr = sdf.format(new java.util.Date());
        String updateStr = uploader.getScoreUpdateDate() == null ? "" : sdf.format(uploader.getScoreUpdateDate());

        int todayAdded = uploader.getTodayAddScore() == null ? 0 : uploader.getTodayAddScore();
        if (!todayStr.equals(updateStr)) {
            todayAdded = 0;
            uploader.setScoreUpdateDate(new java.util.Date());
        }

        int currentScore = uploader.getCreditScore() == null ? 100 : uploader.getCreditScore();
        if (todayAdded < 5 && currentScore < 100) {
            // 交接成功理论加3分
            int canAdd = Math.min(3, 5 - todayAdded);
            int finalScore = Math.min(100, currentScore + canAdd);
            int actualAdded = finalScore - currentScore;

            if (actualAdded > 0) {
                uploader.setCreditScore(finalScore);
                uploader.setTodayAddScore(todayAdded + actualAdded);
                userMapper.updateByPrimaryKeySelective(uploader);
                messageMapper.insert(new Message(uploader.getUserid(), 
                    "【信用奖励】成功交接给下一位书友！本次恢复 " + actualAdded + " 分 (今日已累计恢复 " + (todayAdded + actualAdded) + "/5 分)，当前：" + finalScore + " 分。"));
            }
        }
    }
}
```

---

##### 当日额度控制（每日最多恢复5分）

**后端代码 - BookInfoServiceImpl.java / BorrowServiceImpl.java**
```java
int todayAdded = uploader.getTodayAddScore() == null ? 0 : uploader.getTodayAddScore();
// 如果今天还没加过分，重置今日额度
if (!todayStr.equals(updateStr)) {
    todayAdded = 0;
    uploader.setScoreUpdateDate(new java.util.Date());
}

// 只有今天加分还没满5分，且没满100分时才加
if (todayAdded < 5 && currentScore < 100) {
    // 计算可加分数
    int canAdd = Math.min(奖励分值, 5 - todayAdded);
    // ...
}
```

---

##### 封顶100分

**后端代码**
```java
int finalScore = Math.min(100, currentScore + canAdd);
```

---

#### 1.3.3 信用分惩罚

##### 逾期每日扣 -2分

**后端代码 - BorrowTask.java（定时任务）**
```java
// 2. 巡查【已逾期】(1)，每日扣分
if (borrow.getState() == 1 && borrow.getBorrowtime() != null) {
    int allowDays = (borrow.getBorrowDays() != null) ? borrow.getBorrowDays() : 30;
    long borrowedMillis = now - borrow.getBorrowtime().getTime();
    long allowMillis = (long) allowDays * 24 * 60 * 60 * 1000;

    if (borrowedMillis > allowMillis || allowDays == 0) {
        User borrower = userMapper.selectByPrimaryKey(borrow.getUserid());
        if (borrower != null && borrower.getCreditScore() != null) {
            int currentScore = borrower.getCreditScore();
            if (currentScore > 0) {
                int newScore = Math.max(0, currentScore - 2); // 扣2分
                borrower.setCreditScore(newScore);
                userMapper.updateByPrimaryKeySelective(borrower);

                String msg = "【逾期惩罚】您的借阅已逾期！系统已扣除您 2 信用分。当前剩余：" + newScore + " 分。";
                messageMapper.insert(new Message(borrower.getUserid(), msg));
            }
        }
    }
}
```

---

##### 报损登记扣 -10分

**后端代码 - BorrowServiceImpl.java**
```java
@Override
@Transactional
public R reportLoss(Integer borrowId, String reason) {
    Borrow borrow = borrowMapper.selectByPrimaryKey(borrowId);
    if (borrow == null) return R.error("记录不存在");

    borrow.setState(6);
    borrow.setReturntime(new Date());
    borrow.setReturnmsg("【遗失登记】" + reason);
    borrowMapper.updateByPrimaryKeySelective(borrow);

    BookInfo book = bookInfoMapper.selectByPrimaryKey(borrow.getBookid());
    book.setInventory(-1);
    book.setIsborrowed(1);
    book.setContactinfo("此书已遗憾退出漂流");
    bookInfoMapper.updateByPrimaryKeySelective(book);

    // 【新增惩罚机制：扣除10信用分】
    User badUser = userMapper.selectByPrimaryKey(borrow.getUserid());
    if (badUser != null) {
        int currentScore = (badUser.getCreditScore() != null) ? badUser.getCreditScore() : 100;
        badUser.setCreditScore(Math.max(0, currentScore - 10)); // 每次遗失扣除 10 分
        userMapper.updateByPrimaryKeySelective(badUser);

        if (badUser.getOpenId() != null) {
            WechatPushUtils.pushMessage(
                badUser.getOpenId(),
                "⛔ 信用降级通知",
                "因您主动向发布者登记所借阅的书籍《" + book.getBookname() + "》已遗失，系统依照惩罚规则，已自动扣除您 10 信誉分。<br/>您当前的剩余信誉分将变为：" + badUser.getCreditScore() + "分。"
            );
        }
    }

    messageMapper.insert(new Message(borrow.getUserid(), "【扣分通知】因登记书籍遗失，系统已自动扣除您 10 信誉分。保护好每一本书是对漂流社区的尊重！"));
    return R.ok("登记遗失成功，并已扣除您 10 信誉分作为惩罚！该书已终止漂流");
}
```

---

##### 强制收回扣 -10分

**后端代码 - BorrowServiceImpl.java**
```java
// 【全新风控毒点】：逾期强制收回并扣除信用分 (温和版)
@Override
@Transactional
public R forcePenalize(Integer borrowId) {
    Borrow borrow = borrowMapper.selectByPrimaryKey(borrowId);
    if (borrow == null || borrow.getState() != 1) return R.error("当前状态无法执行强制收回");

    BookInfo book = bookInfoMapper.selectByPrimaryKey(borrow.getBookid());
    User badUser = userMapper.selectByPrimaryKey(borrow.getUserid());

    // 1. 温和扣分：只扣除 10 信用分
    if (badUser != null) {
        int currentScore = (badUser.getCreditScore() != null) ? badUser.getCreditScore() : 100;
        badUser.setCreditScore(Math.max(0, currentScore - 10)); // 最低扣到 0 分
        userMapper.updateByPrimaryKeySelective(badUser);
    }

    // 2. 强制结束本次借阅 (设为状态7-逾期强裁)
    borrow.setState(7);
    borrow.setReturntime(new Date());
    borrow.setReturnmsg("【系统强制收回】因逾期未还且无视催还，已扣除 10 信用分");
    borrowMapper.updateByPrimaryKeySelective(borrow);

    // 3. 恢复图书库存，重新上架
    if (book != null) {
        book.setInventory(book.getInventory() + 1);
        if (book.getInventory() > 0) book.setIsborrowed(0);
        bookInfoMapper.updateByPrimaryKeySelective(book);
    }

    // 4. 微信通知
    if (badUser != null && badUser.getOpenId() != null) {
        WechatPushUtils.pushMessage(
            badUser.getOpenId(),
            "⛔ 信用降级与强制收回通知",
            "警告！因您逾期未归还《" + book.getBookname() + "》，系统已强制结束借阅，并扣除您 10 信用分！<br/>当前剩余信用分：" + badUser.getCreditScore() + "分。<br/>⚠️ 若低于 60 分将永久封禁借阅权限！"
        );
    }

    return R.ok("已成功强制收回，并扣除对方 10 信用分！");
}
```

---

##### 最低0分

**后端代码**
```java
badUser.setCreditScore(Math.max(0, currentScore - 10)); // 最低扣到 0 分
```

---

## 二、图书模块

```
图书模块 
 ├── 图书发布 
 │   ├── 图书信息录入 
 │   │   ├── 书名、作者、分类录入 
 │   │   ├── 寄语简介填写 
 │   │   ├── 交接说明填写 
 │   │   └── 封面图片上传 
 │   ├── 库存设置 
 │   │   ├── 设置书籍总数 
 │   │   └── 自动同步库存 
 │   └── 发布审核 
 │       ├── 默认待审核状态 
 │       ├── 管理员审核通过 
 │       └── 管理员审核驳回 
 ├── 图书查询 
 │   ├── 分页查询 
 │   │   ├── 书名模糊搜索 
 │   │   ├── 作者模糊搜索 
 │   │   └── 分类筛选 
 │   ├── 我的发布 
 │   │   ├── 查看自己发布的图书 
 │   │   └── 查看审核状态 
 │   └── 漂流足迹 
 │       ├── 查看图书流转历史 
 │       └── 查看借阅者信息 
 └── 图书管理 
     ├── 图书编辑 
     │   ├── 修改图书信息 
     │   └── 漂流中禁止修改 
     └── 图书下架 
         ├── 删除图书 
         └── 漂流中禁止删除
```

---

### 2.1 图书发布

#### 2.1.1 图书信息录入

##### 书名、作者、分类录入

**前端代码 - bookinfo/index.vue**
```vue
<el-form :model="form" ref="ruleForm" :rules="rules" label-width="90px">
  <el-form-item label="图书名称" prop="bookname">
    <el-input v-model="form.bookname" placeholder="书名" />
  </el-form-item>
  <el-form-item label="图书作者" prop="bookauthor">
    <el-input v-model="form.bookauthor" placeholder="作者" />
  </el-form-item>
  <el-form-item label="图书分类" prop="booktypeid">
    <el-select v-model="form.booktypeid" style="width: 100%">
      <el-option v-for="item in typeData" :key="item.booktypeid" :label="item.booktypename" :value="item.booktypeid"/>
    </el-select>
  </el-form-item>
</el-form>
```

**后端代码 - BookInfoController.java**
```java
@PostMapping(value = "/addBookInfo")
public Integer addBookInfo(@RequestBody BookInfo bookInfo) {
    return bookInfoService.addBookInfo(bookInfo);
}
```

---

##### 寄语简介填写

**前端代码 - bookinfo/index.vue**
```vue
<el-form-item label="寄语简介" prop="bookdesc">
  <el-input type="textarea" v-model="form.bookdesc" :rows="2" placeholder="谈谈读书感悟（所有人可见，选填）" />
</el-form-item>
```

---

##### 交接说明填写

**前端代码 - bookinfo/index.vue**
```vue
<el-form-item label="交接说明" prop="contactinfo">
  <el-input type="textarea" v-model="form.contactinfo" :rows="2" placeholder="微信号/宿舍号（必须填写，仅审核通过后可见）" />
</el-form-item>
```

**表单验证规则**
```javascript
rules: {
  bookname: [{ required: true, message: '请填写图书名称', trigger: 'blur' }],
  bookauthor: [{ required: true, message: '请填写图书作者', trigger: 'blur' }],
  booktypeid: [{ required: true, message: '请选择图书分类', trigger: 'change' }],
  bookcount: [{ required: true, message: '请填写书籍总数', trigger: 'blur' }],
  contactinfo: [{ required: true, message: '请填写交接说明(必须填写)', trigger: 'blur' }]
}
```

---

##### 封面图片上传

**前端代码 - bookinfo/index.vue**
```vue
<div class="side-upload-panel">
  <span class="upload-title">图书封面</span>
  <el-upload 
    class="book-uploader" 
    action="http://localhost:9111/BookManager/upload/uploadImg" 
    :show-file-list="false" 
    :on-success="handleAvatarSuccess">
    <img v-if="form.bookimg" :src="'http://localhost:9111/BookManager' + form.bookimg" class="cover-image" />
    <div v-else class="upload-placeholder">
      <i class="el-icon-picture-outline"></i>
      <p>添加封面图</p>
    </div>
  </el-upload>
</div>
```

```javascript
handleAvatarSuccess(res) { 
  if (res.code === 0) { 
    this.form.bookimg = res.data; 
    this.$message.success("封面上传成功"); 
  } 
}
```

---

#### 2.1.2 库存设置

##### 设置书籍总数

**前端代码 - bookinfo/index.vue**
```vue
<el-form-item label="书籍总数" prop="bookcount">
  <el-input-number v-model="form.bookcount" :min="1" style="width: 100%" />
</el-form-item>
```

---

##### 自动同步库存

**后端代码 - BookInfoServiceImpl.java**
```java
@Override
public Integer addBookInfo(BookInfo bookInfo) {
    if (bookInfo.getBookcount() != null && bookInfo.getInventory() == null) {
        bookInfo.setInventory(bookInfo.getBookcount());
    }
    if (bookInfo.getIsborrowed() == null) {
        bookInfo.setIsborrowed(0);
    }
    
    // 刚发布的图书默认为待审核状态
    if (bookInfo.getAuditstatus() == null) {
        bookInfo.setAuditstatus(0);
    }

    int result = bookInfoMapper.insert(bookInfo);
    // ... 后续处理
    return result;
}
```

---

#### 2.1.3 发布审核

##### 默认待审核状态

**后端代码 - BookInfoServiceImpl.java**
```java
// 刚发布的图书默认为待审核状态
if (bookInfo.getAuditstatus() == null) {
    bookInfo.setAuditstatus(0);
}
```

---

##### 管理员审核通过

**后端代码 - BookInfoController.java**
```java
@PostMapping(value = "/audit")
public Integer auditBook(@RequestParam Integer bookid, @RequestParam Integer auditstatus, @RequestParam(required = false) String reason) {
    return bookInfoService.auditBook(bookid, auditstatus, reason);
}
```

**后端代码 - BookInfoServiceImpl.java**
```java
@Override
public Integer auditBook(Integer bookid, Integer auditstatus, String reason) {
    BookInfo book = bookInfoMapper.selectByPrimaryKey(bookid);
    if (book == null) return 0;
    
    book.setAuditstatus(auditstatus);
    int result = bookInfoMapper.updateByPrimaryKeySelective(book);
    
    if (result > 0) {
        String msg = auditstatus == 1 ? 
            "【系统通知】您的书籍《" + book.getBookname() + "》已通过审核并上架！" : 
            "【系统通知】您的书籍《" + book.getBookname() + "》审核未通过。原因：" + (reason == null || reason.isEmpty() ? "无" : reason);
        messageMapper.insert(new Message(book.getUploaderid(), msg));
        
        User uploader = userMapper.selectByPrimaryKey(book.getUploaderid());
        if (uploader != null && uploader.getOpenId() != null) {
            if (auditstatus == 1) {
                WechatPushUtils.pushMessage(uploader.getOpenId(), "书籍审核通过", "您的《" + book.getBookname() + "》现已成功上架！");
            } else if (auditstatus == 2) {
                WechatPushUtils.pushMessage(uploader.getOpenId(), "书籍发布被驳回", "您的《" + book.getBookname() + "》审核未通过，原因：" + (reason == null || reason.isEmpty() ? "无" : reason));
            }
        }
    }
    return result;
}
```

---

##### 管理员审核驳回

**后端代码 - BookInfoServiceImpl.java**
```java
String msg = auditstatus == 2 ? 
    "【系统通知】您的书籍《" + book.getBookname() + "》审核未通过。原因：" + (reason == null || reason.isEmpty() ? "无" : reason) : 
    "...";
messageMapper.insert(new Message(book.getUploaderid(), msg));

if (auditstatus == 2) {
    WechatPushUtils.pushMessage(uploader.getOpenId(), "书籍发布被驳回", 
        "您的《" + book.getBookname() + "》审核未通过，原因：" + (reason == null || reason.isEmpty() ? "无" : reason));
}
```

---

### 2.2 图书查询

#### 2.2.1 分页查询

##### 书名模糊搜索

**前端代码 - bookinfo/index.vue**
```vue
<el-form-item>
  <el-input v-model="queryParam.bookname" placeholder="书名检索..." prefix-icon="el-icon-search" clearable />
</el-form-item>
```

**后端代码 - BookInfoController.java**
```java
@GetMapping(value = "/queryBookInfosByPage")
public Map<String, Object> queryBookInfosByPage(@RequestParam Map<String, Object> params) {
    PageUtils.parsePageParams(params);
    int count = bookInfoService.getSearchCount(params);
    List<BookInfo> bookInfos = bookInfoService.searchBookInfosByPage(params);
    return R.getListResultMap(0, "success", count, bookInfos);
}
```

---

##### 作者模糊搜索

**前端代码 - bookinfo/index.vue**
```vue
<el-form-item>
  <el-input v-model="queryParam.bookauthor" placeholder="作者..." prefix-icon="el-icon-user" clearable />
</el-form-item>
```

---

##### 分类筛选

**前端代码 - bookinfo/index.vue**
```vue
<el-form-item>
  <el-select v-model="queryParam.booktypeid" placeholder="图书分类" clearable>
    <el-option v-for="item in typeData" :key="item.booktypeid" :label="item.booktypename" :value="item.booktypeid"/>
  </el-select>
</el-form-item>
```

---

#### 2.2.2 我的发布

##### 查看自己发布的图书

**前端代码 - bookinfo/index.vue**
```javascript
handleMyPublished() {
  this.queryParam = { page: 1, limit: 12, bookname: null, bookauthor: null, booktypeid: null, uploaderId: this.id, auditstatus: null };
  this.fetchData();
}
```

```vue
<el-button type="warning" plain icon="el-icon-user" @click="handleMyPublished" class="modern-btn">我发布的</el-button>
```

---

##### 查看审核状态

**前端代码 - bookinfo/index.vue**
```vue
<div class="status-tag" :class="{'out': item.inventory <= 0 || item.auditstatus !== 1}">
  <span v-if="item.auditstatus === 0">待审核</span>
  <span v-else-if="item.auditstatus === 2">审核驳回</span>
  <span v-else>{{ item.inventory > 0 ? '剩余 ' + item.inventory : '漂流中' }}</span>
</div>
```

---

#### 2.2.3 漂流足迹

##### 查看图书流转历史

**前端代码 - bookinfo/index.vue**
```vue
<el-dialog title="🗺️ 书籍漂流足迹" :visible.sync="footprintDialogVisible" width="500px">
  <div class="footprint-scroll-box">
    <el-timeline v-if="footprintList.length > 0">
      <el-timeline-item v-for="(activity, index) in footprintList" :key="index" 
        :icon="activity.state === 2 ? 'el-icon-check' : (activity.state === 1 ? 'el-icon-reading' : 'el-icon-info')" 
        :type="activity.state === 2 ? 'success' : (activity.state === 1 ? 'primary' : 'info')" 
        :timestamp="activity.applytime">
        <span style="font-weight: bold; color: #409EFF;">{{ activity.username }}</span>
        <span v-if="activity.state === 0"> 提交了漂流申请，等待审核...</span>
        <span v-else-if="activity.state === 3"> 的申请被驳回了</span>
        <span v-else-if="activity.state === 4"> 审核通过，正在等待线下交接</span>
        <span v-else-if="activity.state === 5"> 撤销了本次借阅</span>
        <span v-else-if="activity.state === 1"> 正在阅读中...</span>
        <span v-else-if="activity.state === 2"> 读完了这本书</span>
      </el-timeline-item>
    </el-timeline>
    <el-empty v-else description="这本书还没有开始它的漂流之旅哦"></el-empty>
  </div>
</el-dialog>
```

```javascript
openFootprint(row) {
  this.footprintDialogVisible = true; 
  this.footprintList = [];
  request({ url: '/borrow/queryBorrowsByPage', method: 'get', params: { page: 1, limit: 100, bookId: row.bookid } }).then(res => {
    if (res.data) { this.footprintList = res.data; } 
    else if (res.rows) { this.footprintList = res.rows; }
  });
}
```

---

### 2.3 图书管理

#### 2.3.1 图书编辑

##### 修改图书信息

**前端代码 - bookinfo/index.vue**
```javascript
handleUpdate(row) { 
  if (row.inventory < row.bookcount) {
    this.$message.warning("该书籍正在漂流交接中或已被借阅，暂时无法修改信息！");
    return;
  }
  this.formType = 1; 
  this.form = { ...row }; 
  this.dialogFormVisible = true; 
}
```

**后端代码 - BookInfoController.java**
```java
@PutMapping(value = "/updateBookInfo")
public Integer updateBookInfo(@RequestBody BookInfo bookInfo) {
    return bookInfoService.updateBookInfo(bookInfo);
}
```

---

##### 漂流中禁止修改

**前端代码 - bookinfo/index.vue**
```javascript
handleUpdate(row) { 
  if (row.inventory < row.bookcount) {
    this.$message.warning("该书籍正在漂流交接中或已被借阅，暂时无法修改信息！");
    return;
  }
  // ... 允许修改
}
```

---

#### 2.3.2 图书下架

##### 删除图书

**前端代码 - bookinfo/index.vue**
```javascript
handleDelete(row) {
  if (row.inventory < row.bookcount) {
    this.$message.warning("该书籍正在漂流交接中或已被借阅，无法下架！");
    return;
  }
  this.$confirm("确定要下架这本图书吗?", "提示", { type: 'warning' }).then(() => {
    deleteBookInfo(row).then((res) => { 
      if (res === 1) { this.$message.success("下架成功"); this.fetchData(); } 
    });
  });
}
```

**后端代码 - BookInfoController.java**
```java
@DeleteMapping(value = "/deleteBookInfo")
public Integer deleteBookInfo(@RequestBody BookInfo bookInfo) {
    return bookInfoService.deleteBookInfo(bookInfo);
}
```

---

##### 漂流中禁止删除

**前端代码 - bookinfo/index.vue**
```javascript
handleDelete(row) {
  if (row.inventory < row.bookcount) {
    this.$message.warning("该书籍正在漂流交接中或已被借阅，无法下架！");
    return;
  }
  // ... 允许删除
}
```

---

## 三、借阅模块

```
借阅模块 
 ├── 借阅申请 
 │   ├── 申请漂流 
 │   │   ├── 填写申请理由 
 │   │   ├── 设置借阅天数 
 │   │   ├── 信用分校验（≥60可借） 
 │   │   ├── 禁止借阅自己发布的书 
 │   │   └── 管理员禁止借书 
 │   └── 申请通知 
 │       ├── 通知发布者 
 │       └── 微信推送通知 
 ├── 借阅审核 
 │   ├── 审核通过 
 │   │   ├── 扣减库存 
 │   │   ├── 生成交接暗号 
 │   │   ├── 通知申请人 
 │   │   └── 微信推送通知 
 │   └── 审核驳回 
 │       ├── 记录驳回原因 
 │       ├── 通知申请人 
 │       └── 微信推送通知 
 ├── 交接管理 
 │   ├── 暗号核销 
 │   │   ├── 验证6位暗号 
 │   │   ├── 信用分二次校验 
 │   │   ├── 状态变更为"漂流中" 
 │   │   └── 发布者信用分奖励 
 │   ├── 协商交接 
 │   │   ├── 发送协商消息 
 │   │   └── 微信推送通知 
 │   └── 撤销交接 
 │       ├── 恢复库存 
 │       ├── 状态变更为"已撤销" 
 │       └── 双向通知 
 ├── 归还传递 
 │   ├── 填写漂流感悟 
 │   ├── 填写交接说明 
 │   ├── 持书人变更 
 │   └── 库存恢复 
 ├── 报损登记 
 │   ├── 状态变更为"已遗失" 
 │   ├── 扣除10信用分 
 │   ├── 书籍退出漂流 
 │   └── 双向通知 
 └── 超时处理 
     ├── 超时未审核自动取消 
     ├── 超时未交接自动释放 
     └── 逾期每日扣分
```

---

### 3.1 借阅申请

#### 3.1.1 申请漂流

##### 填写申请理由

**前端代码 - bookinfo/index.vue**
```vue
<el-dialog title="📚 提交漂流申请" :visible.sync="borrowDialogVisible" width="500px">
  <el-form :model="borrowForm" label-position="top">
    <el-form-item label="阅读计划 / 申请理由">
      <el-input type="textarea" v-model="borrowForm.borrowreason" :rows="3" 
        placeholder="请真诚地填写您的申请理由，这会大大提高通过率哦..." />
    </el-form-item>
  </el-form>
</el-dialog>
```

---

##### 设置借阅天数

**前端代码 - bookinfo/index.vue**
```vue
<el-form-item>
  <template slot="label">
    预计借阅天数 <span style="color: #909399; font-size: 12px;">(可自由填入，最高 365 天)</span>
  </template>
  <el-input-number v-model="borrowForm.borrowDays" :min="0" :max="365" style="width: 100%;"></el-input-number>
  <div style="font-size: 12px; color: #F56C6C; margin-top: 5px;" v-if="borrowForm.borrowDays === 0">
    <i class="el-icon-warning"></i> 提示：设为 0 天为测试模式，书籍交付后将立刻触发逾期。
  </div>
</el-form-item>
```

---

##### 信用分校验（≥60可借）

**后端代码 - BorrowServiceImpl.java**
```java
@Override
@Transactional
public R addBorrow(Borrow borrow) {
    User currentUser = userMapper.selectByPrimaryKey(borrow.getUserid());

    if (currentUser != null && currentUser.getCreditScore() != null && currentUser.getCreditScore() < 60) {
        return R.error("您的信用分低于 60 分，已被限制借阅！如有特殊情况请线下联系管理员。");
    }
    // ...
}
```

---

##### 禁止借阅自己发布的书

**后端代码 - BorrowServiceImpl.java**
```java
// 【风控拦截 2】：禁止自己借阅自己的书籍！
if (book.getUploaderid() != null && book.getUploaderid().equals(borrow.getUserid())) {
    return R.error("不能申请借阅自己发布（或当前持有）的书籍哦！");
}
```

---

##### 管理员禁止借书

**后端代码 - BorrowServiceImpl.java**
```java
// 【新增】：管理员不可借书！
if (currentUser != null && currentUser.getIsadmin() != null && currentUser.getIsadmin() == 1) {
    return R.error("管理员账号为后台管理专用，不可参与前端借阅流转！");
}
```

---

#### 3.1.2 申请通知

##### 通知发布者

**后端代码 - BorrowServiceImpl.java**
```java
// 有人申请时，立刻通知发布者！
if (row > 0 && book.getUploaderid() != null) {
    String msgContent = "【系统通知】有同学申请借阅了您发布的书籍《" + book.getBookname() + "》！\n" +
            "TA 的申请理由是：[" + borrow.getBorrowreason() + "]\n" +
            "目前正在等待管理员审核，请留意后续通知。";
    messageMapper.insert(new Message(book.getUploaderid(), msgContent));
}
```

---

##### 微信推送通知

**后端代码 - BorrowServiceImpl.java**
```java
// 【WxPusher 场景1：有人申请借书】
User uploader = userMapper.selectByPrimaryKey(book.getUploaderid());
if (uploader != null && uploader.getOpenId() != null) {
    WechatPushUtils.pushMessage(
            uploader.getOpenId(),
            "📚 新的漂流申请",
            "您好！您发布的《" + book.getBookname() + "》有人申请啦！<br/>留言：" + borrow.getBorrowreason() + "<br/>请尽快登录系统进行审核！"
    );
}
```

---

### 3.2 借阅审核

#### 3.2.1 审核通过

##### 扣减库存

**后端代码 - BorrowServiceImpl.java**
```java
if (state == 1) { // 审核通过
    if (book.getInventory() <= 0) return R.error("审核失败：库存不足");

    book.setInventory(book.getInventory() - 1);
    if (book.getInventory() == 0) book.setIsborrowed(1); // 标记为无货
    bookInfoMapper.updateByPrimaryKeySelective(book);
    // ...
}
```

---

##### 生成交接暗号

**后端代码 - BorrowServiceImpl.java**
```java
String secretCode = String.valueOf((int)((Math.random() * 9 + 1) * 100000));
borrow.setState(4); // 4-待交接
borrow.setApplytime(new Date()); // 倒计时重置
borrow.setSecretCode(secretCode);
borrowMapper.updateByPrimaryKeySelective(borrow);
```

---

##### 通知申请人

**后端代码 - BorrowServiceImpl.java**
```java
String contact = (book.getContactinfo() != null && !book.getContactinfo().isEmpty()) ? book.getContactinfo() : "无特殊说明，请当面沟通。";
String msgToApplicant = "【系统通知】你的漂流申请《" + book.getBookname() + "》已通过！\n" +
        "对方交接说明：【" + contact + "】\n" +
        "你的专属提货暗号为：【" + secretCode + "】。碰面拿到书后，请将此暗号出示给发布者，由发布者在系统中进行核销。";
messageMapper.insert(new Message(borrow.getUserid(), msgToApplicant));
```

---

##### 微信推送通知

**后端代码 - BorrowServiceImpl.java**
```java
User applyUser = userMapper.selectByPrimaryKey(borrow.getUserid());
if (applyUser != null && applyUser.getOpenId() != null) {
    WechatPushUtils.pushMessage(applyUser.getOpenId(), "✅ 审核通过通知", "您对《" + book.getBookname() + "》的借阅申请已通过！请准备线下交接。");
}
```

---

#### 3.2.2 审核驳回

##### 记录驳回原因

**后端代码 - BorrowServiceImpl.java**
```java
} else {
    borrow.setState(3); // 3-驳回
    String reason = feedback != null ? feedback : "无";
    borrow.setReturnmsg("【发布者驳回】" + reason); // 【展示死因】
    borrowMapper.updateByPrimaryKeySelective(borrow);
    // ...
}
```

---

##### 通知申请人

**后端代码 - BorrowServiceImpl.java**
```java
Message msg = new Message(borrow.getUserid(), "【系统通知】您的申请《" + book.getBookname() + "》被驳回。原因：" + reason);
messageMapper.insert(msg);
```

---

##### 微信推送通知

**后端代码 - BorrowServiceImpl.java**
```java
User applyUser = userMapper.selectByPrimaryKey(borrow.getUserid());
if (applyUser != null && applyUser.getOpenId() != null) {
    WechatPushUtils.pushMessage(applyUser.getOpenId(), "❌ 审核驳回通知", "对《" + book.getBookname() + "》的申请被拒绝。<br/>原因：" + reason);
}
```

---

### 3.3 交接管理

#### 3.3.1 暗号核销

##### 验证6位暗号

**前端代码 - borrow/index.vue**
```vue
<el-dialog :visible.sync="dialogVisible" width="420px">
  <div class="dialog-header-custom">
    <h3>面对面交接验证</h3>
    <p>请输入借阅者出示的 6 位数字暗号，确认交接</p>
  </div>
  <el-input v-model="inputSecretCode" placeholder="··· ···" maxlength="6" class="secret-input" />
  <div slot="footer">
    <el-button round @click="dialogVisible = false">取消</el-button>
    <el-button type="primary" round @click="submitHandover" :loading="submitLoading">核 销 暗 号</el-button>
  </div>
</el-dialog>
```

```javascript
submitHandover() {
  if (this.inputSecretCode.length !== 6) return this.$message.warning("请输入完整的 6 位交接暗号！");
  this.submitLoading = true;
  request({ url: '/borrow/verifyCode', method: 'post', params: { borrowId: this.currentRowId, secretCode: this.inputSecretCode } }).then(res => {
    this.submitLoading = false;
    if (res.code === 0 || res === 1) { 
      this.$message.success("🎉 暗号核销正确！交接成功！"); 
      this.dialogVisible = false; 
      this.getList(); 
    }
    else { this.$message.error(res.msg || "暗号错误！"); }
  });
}
```

**后端代码 - BorrowServiceImpl.java**
```java
@Override
@Transactional
public R verifyCode(Integer borrowId, String secretCode) {
    Borrow borrow = borrowMapper.selectByPrimaryKey(borrowId);
    if (borrow == null) return R.error("记录不存在");
    if (borrow.getState() != 4) return R.error("当前状态不支持验证暗号");
    if (borrow.getSecretCode() == null || !borrow.getSecretCode().equals(secretCode)) {
        return R.error("暗号错误，请核对后再试！");
    }
    // ...
}
```

---

##### 信用分二次校验

**后端代码 - BorrowServiceImpl.java**
```java
// 漏洞防范：核销时如果变成老赖，直接阻断！
User borrower = userMapper.selectByPrimaryKey(borrow.getUserid());
if (borrower != null && borrower.getCreditScore() != null && borrower.getCreditScore() < 60) {
    return R.error("阻断交易！该借阅者近期违规信用已跌破60，系统强制终止交接！");
}
```

---

##### 状态变更为"漂流中"

**后端代码 - BorrowServiceImpl.java**
```java
borrow.setState(1);
borrow.setBorrowtime(new Date());
borrowMapper.updateByPrimaryKeySelective(borrow);
return R.ok("暗号正确！交接成功，书籍正式进入漂流中。");
```

---

##### 发布者信用分奖励

**后端代码 - BorrowServiceImpl.java**
```java
// 【成功交接回血策略】
BookInfo book = bookInfoMapper.selectByPrimaryKey(borrow.getBookid());
if (book != null && book.getUploaderid() != null) {
    User uploader = userMapper.selectByPrimaryKey(book.getUploaderid());
    if (uploader != null && (uploader.getCreditScore() == null || uploader.getCreditScore() < 100)) {
        // ... 交接成功加3分逻辑
        int canAdd = Math.min(3, 5 - todayAdded);
        int finalScore = Math.min(100, currentScore + canAdd);
        int actualAdded = finalScore - currentScore;

        if (actualAdded > 0) {
            uploader.setCreditScore(finalScore);
            uploader.setTodayAddScore(todayAdded + actualAdded);
            userMapper.updateByPrimaryKeySelective(uploader);
            messageMapper.insert(new Message(uploader.getUserid(), "【信用奖励】成功交接给下一位书友！本次恢复 " + actualAdded + " 分"));
        }
    }
}
```

---

#### 3.3.2 协商交接

##### 发送协商消息

**前端代码 - borrow/index.vue**
```javascript
handleNudge(row) {
  this.$prompt('留下建议碰面地点：', '请求协商交接', { confirmButtonText: '发送', cancelButtonText: '取消' }).then(({ value }) => {
    if (!value) return this.$message.warning("留言不能为空");
    request({ url: '/borrow/nudge', method: 'post', params: { borrowId: row.borrowid, message: value } }).then(res => {
      if (res.code === 0 || res === 1) { this.$message.success('发送成功！'); }
    });
  });
}
```

**后端代码 - BorrowServiceImpl.java**
```java
@Override
@Transactional
public R nudgeUploader(Integer borrowId, String message) {
    Borrow borrow = borrowMapper.selectByPrimaryKey(borrowId);
    if(borrow == null || borrow.getState() != 4) return R.error("当前状态无法发送协商");

    BookInfo book = bookInfoMapper.selectByPrimaryKey(borrow.getBookid());
    if(book == null || book.getUploaderid() == null) return R.error("找不到发布者信息，无法传话");

    String content = "【紧急交接协商】您发布的《" + book.getBookname() + "》的借阅者遇到了交接困难。\n" +
            "TA 的紧急留言是：【" + message + "】\n" +
            "请您通过上述方式联系 TA，或约定一个新的公共地点完成交接。";

    messageMapper.insert(new Message(book.getUploaderid(), content));
    // ...
    return R.ok("协商消息已成功发送给发布者！");
}
```

---

##### 微信推送通知

**后端代码 - BorrowServiceImpl.java**
```java
// 【WxPusher 场景4：交接协商通知】
User uploader = userMapper.selectByPrimaryKey(book.getUploaderid());
if (uploader != null && uploader.getOpenId() != null) {
    WechatPushUtils.pushMessage(
            uploader.getOpenId(),
            "🆘 紧急交接协商",
            "借书人遇到了交接困难！<br/>关于书籍《" + book.getBookname() + "》，对方留言内容：" + message + "<br/>请尽快登录系统查看详情并联系对方！"
    );
}
```

---

#### 3.3.3 撤销交接

##### 恢复库存

**后端代码 - BorrowServiceImpl.java**
```java
@Override
@Transactional
public R cancelBorrow(Integer borrowId, String reason) {
    Borrow borrow = borrowMapper.selectByPrimaryKey(borrowId);
    if (borrow == null || (borrow.getState() != 4 && borrow.getState() != 0)) return R.error("当前状态无法撤销");
    BookInfo book = bookInfoMapper.selectByPrimaryKey(borrow.getBookid());

    if (borrow.getState() == 4 && book != null) {
        book.setInventory(book.getInventory() + 1);
        if (book.getInventory() > 0) book.setIsborrowed(0);
        bookInfoMapper.updateByPrimaryKeySelective(book);
    }
    // ...
}
```

---

##### 状态变更为"已撤销"

**后端代码 - BorrowServiceImpl.java**
```java
borrow.setState(5);
borrow.setReturnmsg("【用户放弃】" + reason);
borrowMapper.updateByPrimaryKeySelective(borrow);
```

---

##### 双向通知

**后端代码 - BorrowServiceImpl.java**
```java
String stageStr = (borrow.getState() == 0) ? "【申请阶段】" : "【交接阶段】";
messageMapper.insert(new Message(borrow.getUserid(), "【系统通知】您已撤销对《" + book.getBookname() + "》的借阅。"));

if (book != null && book.getUploaderid() != null) {
    messageMapper.insert(new Message(book.getUploaderid(), "【通知】对方在" + stageStr + "撤销了借阅。原因：[" + reason + "]"));
    User uploader = userMapper.selectByPrimaryKey(book.getUploaderid());
    if (uploader != null && uploader.getOpenId() != null) {
        WechatPushUtils.pushMessage(uploader.getOpenId(), "⚠️ 借阅撤销", "对方取消了《" + book.getBookname() + "》的借阅。<br/>对方留言：" + reason);
    }
}
return R.ok("撤销成功！");
```

---

### 3.4 归还传递

#### 填写漂流感悟

**前端代码 - borrow/index.vue**
```vue
<el-dialog title="📚 读完并传递 (成为新一任持书人)" :visible.sync="returnDialogVisible" width="550px">
  <el-form :model="returnForm" label-position="top">
    <el-form-item label="您的漂流感悟 (所有人可见的漂流足迹)">
      <el-input type="textarea" :rows="3" v-model="returnForm.returnMsg" 
        placeholder="这本书给你带来了什么启发？留下你的足迹吧..." />
    </el-form-item>
  </el-form>
</el-dialog>
```

---

#### 填写交接说明

**前端代码 - borrow/index.vue**
```vue
<el-form-item label="您的交接说明 (非常重要！绝对保密)">
  <el-input type="textarea" :rows="2" v-model="returnForm.contactInfo" 
    placeholder="例如：我是男生，住在5栋302，或者加我微信 xxxx" />
  <div style="font-size: 12px; color: #909399; margin-top: 5px;">
    <i class="el-icon-info"></i> 这本书现在由您保管！请留下您的联系方式，等待下一位有缘人申请。
  </div>
</el-form-item>
```

---

#### 持书人变更

**后端代码 - BorrowServiceImpl.java**
```java
@Override
@Transactional
public Integer returnBook(Integer borrowId, Integer bookId, String returnMsg, String contactInfo) {
    Borrow borrow = borrowMapper.selectByPrimaryKey(borrowId);
    if (borrow == null || borrow.getState() != 1) return 0;

    borrow.setState(2);
    borrow.setReturntime(new Date());
    borrow.setReturnmsg(returnMsg);
    borrowMapper.updateByPrimaryKeySelective(borrow);

    BookInfo book = bookInfoMapper.selectByPrimaryKey(bookId);
    if (book != null) {
        Integer oldUploaderId = book.getUploaderid();
        book.setInventory(book.getInventory() + 1);
        if (book.getInventory() > 0) book.setIsborrowed(0);

        // 持书人变更：将当前借阅者设为新的发布者
        book.setUploaderid(borrow.getUserid());

        if (contactInfo != null && !contactInfo.trim().isEmpty()) {
            book.setContactinfo(contactInfo);
        } else {
            book.setContactinfo("新主人暂未留下具体地址，请申请后通过系统留言沟通。");
        }
        bookInfoMapper.updateByPrimaryKeySelective(book);

        String newOwnerMsg = "【漂流接力】恭喜！您已读完《" + book.getBookname() + "》。\n" +
                "由于这是漂流书籍，书本物理上现在由您保管。系统已自动将该书转移到了您的【我借出的】列表中。\n" +
                "等待下一位有缘人申请时，您将作为新的传递者与TA当面交接暗号！让知识继续流动吧！";
        messageMapper.insert(new Message(borrow.getUserid(), newOwnerMsg));
    }
    return 1;
}
```

---

#### 库存恢复

**后端代码 - BorrowServiceImpl.java**
```java
book.setInventory(book.getInventory() + 1);
if (book.getInventory() > 0) book.setIsborrowed(0);
bookInfoMapper.updateByPrimaryKeySelective(book);
```

---

### 3.5 报损登记

#### 状态变更为"已遗失"

**后端代码 - BorrowServiceImpl.java**
```java
borrow.setState(6);
borrow.setReturntime(new Date());
borrow.setReturnmsg("【遗失登记】" + reason);
borrowMapper.updateByPrimaryKeySelective(borrow);
```

---

#### 扣除10信用分

**后端代码 - BorrowServiceImpl.java**
```java
// 【新增惩罚机制：扣除10信用分】
User badUser = userMapper.selectByPrimaryKey(borrow.getUserid());
if (badUser != null) {
    int currentScore = (badUser.getCreditScore() != null) ? badUser.getCreditScore() : 100;
    badUser.setCreditScore(Math.max(0, currentScore - 10)); // 每次遗失扣除 10 分
    userMapper.updateByPrimaryKeySelective(badUser);
}
```

---

#### 书籍退出漂流

**后端代码 - BorrowServiceImpl.java**
```java
BookInfo book = bookInfoMapper.selectByPrimaryKey(borrow.getBookid());
book.setInventory(-1);
book.setIsborrowed(1);
book.setContactinfo("此书已遗憾退出漂流");
bookInfoMapper.updateByPrimaryKeySelective(book);
```

---

#### 双向通知

**后端代码 - BorrowServiceImpl.java**
```java
messageMapper.insert(new Message(book.getUploaderid(), "【遗失通知】很遗憾，您发布的《" + book.getBookname() + "》被读者登记为遗失。原因：" + reason));
messageMapper.insert(new Message(borrow.getUserid(), "【扣分通知】因登记书籍遗失，系统已自动扣除您 10 信誉分。保护好每一本书是对漂流社区的尊重！"));

// 【WxPusher 场景5：书籍遗憾遗失】
User uploader = userMapper.selectByPrimaryKey(book.getUploaderid());
if (uploader != null && uploader.getOpenId() != null) {
    WechatPushUtils.pushMessage(
            uploader.getOpenId(),
            "💔 书籍遗憾遗失",
            "十分抱歉，您发布的《" + book.getBookname() + "》被当前借阅人登记为遗失。<br/>原因：" + reason + "<br/>此书已退出漂流系统。"
    );
}
```

---

### 3.6 超时处理

#### 超时未审核自动取消

**后端代码 - BorrowTask.java**
```java
// 1. 巡查【超时未审核】(0) 和【超时未交接】(4)
if ((borrow.getState() == 0 || borrow.getState() == 4) && borrow.getApplytime() != null) {
    // 测试后门：如果申请理由里包含"测试"两个字，超时时间缩短为 3 分钟
    long timeout = threeDaysMillis;
    if (borrow.getBorrowreason() != null && borrow.getBorrowreason().contains("测试")) {
        timeout = 3L * 60 * 1000; // 测试模式：3 分钟
    }

    // 判断是否超过容忍时间
    if (now - borrow.getApplytime().getTime() > timeout) {
        int oldState = borrow.getState();
        borrow.setState(7); // 7-已失效
        borrow.setReturnmsg("【系统强制取消】超出处理期限，系统已自动释放该申请。");
        borrowMapper.updateByPrimaryKeySelective(borrow);

        // 只有【待交接】超时才退回库存
        if (oldState == 4 && book != null) {
            book.setInventory(book.getInventory() + 1);
            if (book.getInventory() > 0) book.setIsborrowed(0);
            bookInfoMapper.updateByPrimaryKeySelective(book);
        }

        // 双向发送站内信
        messageMapper.insert(new Message(borrow.getUserid(), "【系统通知】您对《" + book.getBookname() + "》的申请因超时未处理，已被系统自动取消。"));
        messageMapper.insert(new Message(book.getUploaderid(), "【系统通知】由于超时未处理，针对《" + book.getBookname() + "》的借阅申请已被系统强制释放。"));
    }
}
```

---

#### 超时未交接自动释放

**后端代码 - BorrowTask.java**
```java
// 待交接状态(4)超时也会被自动取消
if (borrow.getState() == 4 && borrow.getApplytime() != null) {
    if (now - borrow.getApplytime().getTime() > timeout) {
        // 退回库存
        book.setInventory(book.getInventory() + 1);
        if (book.getInventory() > 0) book.setIsborrowed(0);
        bookInfoMapper.updateByPrimaryKeySelective(book);
        // ...
    }
}
```

---

#### 逾期每日扣分

**后端代码 - BorrowTask.java**
```java
// 2. 巡查【已逾期】(1)，每日扣分
if (borrow.getState() == 1 && borrow.getBorrowtime() != null) {
    int allowDays = (borrow.getBorrowDays() != null) ? borrow.getBorrowDays() : 30;
    long borrowedMillis = now - borrow.getBorrowtime().getTime();
    long allowMillis = (long) allowDays * 24 * 60 * 60 * 1000;

    if (borrowedMillis > allowMillis || allowDays == 0) {
        User borrower = userMapper.selectByPrimaryKey(borrow.getUserid());
        if (borrower != null && borrower.getCreditScore() != null) {
            int currentScore = borrower.getCreditScore();
            if (currentScore > 0) {
                int newScore = Math.max(0, currentScore - 2); // 扣2分
                borrower.setCreditScore(newScore);
                userMapper.updateByPrimaryKeySelective(borrower);

                String msg = "【逾期惩罚】您的借阅已逾期！系统已扣除您 2 信用分。当前剩余：" + newScore + " 分。";
                messageMapper.insert(new Message(borrower.getUserid(), msg));
            }
        }
    }
}
```

---

## 四、管理员模块

```
管理员模块 
 ├── 用户管理 
 │   ├── 用户查询 
 │   │   ├── 分页查询用户 
 │   │   ├── 姓名学号搜索 
 │   │   └── 系部筛选 
 │   ├── 用户录入 
 │   │   ├── 录入基本信息 
 │   │   ├── 设置身份权限 
 │   │   └── 默认密码123456 
 │   ├── 用户编辑 
 │   │   ├── 修改用户信息 
 │   │   ├── 调整信用分 
 │   │   └── 启用/禁用账号 
 │   └── 用户删除 
 │       ├── 单个删除 
 │       └── 批量删除 
 ├── 图书审核 
 │   ├── 查看待审核图书 
 │   ├── 审核通过 
 │   └── 审核驳回（填写原因） 
 └── 借阅管理 
     ├── 全局借阅查询 
     ├── 强制审核通过/驳回 
     ├── 强制归还 
     └── 强制收回（扣分）
```

---

### 4.1 用户管理

#### 4.1.1 用户查询

##### 分页查询用户

**后端代码 - UserController.java**
```java
@GetMapping(value = "/queryUsersByPage")
public Map<String, Object> queryUsersByPage(@RequestParam Map<String, Object> params) {
    PageUtils.parsePageParams(params);
    int count = userService.getSearchCount(params);
    List<User> list = userService.searchUsersByPage(params);
    return R.getListResultMap(0, "success", count, list);
}
```

**前端代码 - user/index.vue**
```javascript
getList() {
  this.listLoading = true
  queryUsersByPage(this.listQuery).then(res => {
    if(res.data) { this.list = res.data; this.total = res.count || res.total || 0 }
    else if(res.rows) { this.list = res.rows; this.total = res.total }
    else { this.list = res; this.total = res.length }
    this.listLoading = false
  })
}
```

---

##### 姓名学号搜索

**前端代码 - user/index.vue**
```vue
<el-input v-model="listQuery.username" placeholder="搜索姓名或学号..." style="width: 200px;" class="round-input" @keyup.enter.native="handleFilter">
  <i slot="prefix" class="el-input__icon el-icon-search"></i>
</el-input>
<el-button type="primary" class="round-btn" icon="el-icon-search" @click="handleFilter">搜索</el-button>
```

---

##### 系部筛选

**前端代码 - user/index.vue**
```vue
<el-select v-model="listQuery.department" placeholder="按系部筛选" clearable class="round-input" style="width: 180px;" @change="handleFilter">
  <el-option v-for="dept in departments" :key="dept" :label="dept" :value="dept" />
</el-select>
```

```javascript
departments: ['机械工程系', '电气与控制工程系', '大数据与智能工程系', '土木工程系', '地球科学与工程系', '矿业工程系', '管理科学与工程系', '经济与管理系', '艺术与设计科学系', '材料科学与工程系']
```

---

#### 4.1.2 用户录入

##### 录入基本信息

**前端代码 - user/index.vue**
```vue
<el-form ref="dataForm" :rules="rules" :model="temp" label-position="left" label-width="80px">
  <el-form-item label="姓名" prop="username">
    <el-input v-model="temp.username" placeholder="请输入真实姓名" />
  </el-form-item>
  <el-form-item label="学号" prop="studentid">
    <el-input v-model="temp.studentid" placeholder="作为登录账号" :disabled="dialogStatus==='update'" />
  </el-form-item>
  <el-form-item label="系部" prop="department">
    <el-select v-model="temp.department" placeholder="请选择所属系部" style="width: 100%;" clearable>
      <el-option v-for="dept in departments" :key="dept" :label="dept" :value="dept" />
    </el-select>
  </el-form-item>
  <el-form-item label="性别" prop="gender">
    <el-radio-group v-model="temp.gender">
      <el-radio :label="1">男生</el-radio>
      <el-radio :label="0">女生</el-radio>
      <el-radio :label="2">保密</el-radio>
    </el-radio-group>
  </el-form-item>
</el-form>
```

---

##### 设置身份权限

**前端代码 - user/index.vue**
```vue
<el-form-item label="身份" prop="isadmin">
  <el-radio-group v-model="temp.isadmin">
    <el-radio :label="0">普通书友</el-radio>
    <el-radio :label="1">管理员</el-radio>
  </el-radio-group>
</el-form-item>
```

---

##### 默认密码123456

**前端代码 - user/index.vue**
```javascript
createData() {
  this.$refs['dataForm'].validate((valid) => {
    if (valid) {
      this.temp.userpassword = '123456'  // 默认密码
      addUser(this.temp).then(() => {
        this.list.unshift(this.temp)
        this.dialogFormVisible = false
        this.$message.success('添加成功，初始密码 123456')
        this.getList()
      })
    }
  })
}
```

---

#### 4.1.3 用户编辑

##### 修改用户信息

**后端代码 - UserController.java**
```java
@RequestMapping(value = "/updateUser")
public Integer updateUser(@RequestBody User user) {
    return userService.updateUser(user);
}
```

**前端代码 - user/index.vue**
```javascript
updateData() {
  this.$refs['dataForm'].validate((valid) => {
    if (valid) {
      const tempData = Object.assign({}, this.temp)
      updateUser(tempData).then(() => {
        this.dialogFormVisible = false
        this.$message.success('更新成功，信用分及信息已同步修改')
        this.getList()
      })
    }
  })
}
```

---

##### 调整信用分

**前端代码 - user/index.vue**
```vue
<el-form-item label="信用分" v-if="dialogStatus==='update'">
  <el-slider v-model="temp.creditScore" :min="0" :max="100" show-input></el-slider>
  <div style="font-size: 12px; color: #E6A23C; margin-top: -10px;">
    * 逾期被扣除信用的用户，若线下说明情况，管理员可在此为其恢复分数 (默认 100，低于 60 封禁借阅)。
  </div>
</el-form-item>
```

---

##### 启用/禁用账号

**前端代码 - user/index.vue**
```vue
<el-form-item label="状态">
  <el-switch v-model="temp.status" :active-value="1" :inactive-value="2" 
    active-text="正常" inactive-text="禁用" 
    active-color="#13ce66" inactive-color="#ff4949">
  </el-switch>
</el-form-item>
```

---

#### 4.1.4 用户删除

##### 单个删除

**后端代码 - UserController.java**
```java
@DeleteMapping(value = "/deleteUser")
public Integer deleteUser(@RequestBody User user) {
    return userService.deleteUser(user);
}
```

**前端代码 - user/index.vue**
```javascript
handleDelete(row) {
  this.$confirm('确定要删除该用户吗？此操作无法撤销。', '警告', { 
    confirmButtonText: '确定删除', cancelButtonText: '取消', type: 'warning' 
  }).then(() => {
    deleteUser({ userid: row.userid }).then(() => { 
      this.$message.success('删除成功'); 
      this.getList() 
    })
  })
}
```

---

##### 批量删除

**后端代码 - UserController.java**
```java
@DeleteMapping(value = "/deleteUsers")
public Integer deleteUsers(@RequestBody List<User> users) {
    return userService.deleteUsers(users);
}
```

**后端代码 - UserServiceImpl.java**
```java
@Override
public Integer deleteUsers(List<User> users) {
    int count = 0;
    for(User u : users) {
        count += userMapper.deleteByPrimaryKey(u.getUserid());
    }
    return count;
}
```

---

### 4.2 图书审核

#### 查看待审核图书

**前端代码 - bookinfo/index.vue**
```vue
<div class="status-tag" :class="{'out': item.inventory <= 0 || item.auditstatus !== 1}">
  <span v-if="item.auditstatus === 0">待审核</span>
  <span v-else-if="item.auditstatus === 2">审核驳回</span>
  <span v-else>{{ item.inventory > 0 ? '剩余 ' + item.inventory : '漂流中' }}</span>
</div>
```

---

#### 审核通过

**后端代码 - BookInfoController.java**
```java
@PostMapping(value = "/audit")
public Integer auditBook(@RequestParam Integer bookid, @RequestParam Integer auditstatus, @RequestParam(required = false) String reason) {
    return bookInfoService.auditBook(bookid, auditstatus, reason);
}
```

---

#### 审核驳回（填写原因）

**后端代码 - BookInfoServiceImpl.java**
```java
if (auditstatus == 2) {
    WechatPushUtils.pushMessage(uploader.getOpenId(), "书籍发布被驳回", 
        "您的《" + book.getBookname() + "》审核未通过，原因：" + (reason == null || reason.isEmpty() ? "无" : reason));
}
```

---

### 4.3 借阅管理

#### 全局借阅查询

**前端代码 - borrow/index.vue**
```vue
<div v-if="checkPermission(['admin'])" class="admin-view">
  <el-card class="filter-container" shadow="never">
    <div class="filter-header">
      <span class="filter-title">📚 全局借阅与交接管理</span>
      <div class="filter-actions">
        <el-input v-model="listQuery.username" placeholder="搜索借阅人..." style="width: 150px;" clearable />
        <el-input v-model="listQuery.bookname" placeholder="搜索书名..." style="width: 150px;" clearable />
        <el-button type="primary" icon="el-icon-search" @click="handleFilter">查找记录</el-button>
      </div>
    </div>
  </el-card>

  <el-table :data="list" v-loading="listLoading">
    <el-table-column label="ID" prop="borrowid" width="60" align="center" />
    <el-table-column label="借阅书籍" min-width="160">
      <template slot-scope="{row}">
        <span>{{ row.bookname }}</span>
      </template>
    </el-table-column>
    <el-table-column label="接收人" width="120" align="center">
      <template slot-scope="{row}">
        <el-tag size="small">{{ row.username }}</el-tag>
      </template>
    </el-table-column>
    <el-table-column label="流转状态" width="100" align="center">
      <template slot-scope="{row}">
        <el-tag v-if="row.state === 0" type="info" size="small">待审核</el-tag>
        <el-tag v-else-if="row.state === 1" type="primary" size="small">漂流中</el-tag>
        <el-tag v-else-if="row.state === 2" type="success" size="small">已归还</el-tag>
        <el-tag v-else-if="row.state === 3" type="danger" size="small">已驳回</el-tag>
        <el-tag v-else-if="row.state === 4" type="warning" size="small">⏳ 待交接</el-tag>
      </template>
    </el-table-column>
  </el-table>
</div>
```

---

#### 强制审核通过/驳回

**前端代码 - borrow/index.vue**
```vue
<template v-if="row.state === 0">
  <el-button size="mini" type="success" round @click="handleAudit(row, 1)">同意</el-button>
  <el-button size="mini" type="danger" plain round @click="handleAudit(row, 3)">驳回</el-button>
</template>
```

---

#### 强制归还

**前端代码 - borrow/index.vue**
```vue
<el-button v-else-if="row.state === 1" size="mini" type="danger" round plain @click="handleReturn(row)">强制归还</el-button>
```

---

#### 强制收回（扣分）

**前端代码 - borrow/index.vue**
```javascript
// 管理员可调用强制收回接口
```

**后端代码 - BorrowController.java**
```java
@PostMapping("/forcePenalize")
public R forcePenalize(@RequestParam Integer borrowId) {
    return borrowService.forcePenalize(borrowId);
}
```

---

## 五、实体类与数据模型

### User 实体类

```java
package com.shanzhu.book.model;

import lombok.Data;
import java.io.Serializable;

@Data
public class User implements Serializable {
    private Integer userid;
    private String username;       // 姓名
    private String studentid;      // 学号
    private String userpassword;   // 密码
    private Integer isadmin;       // 身份：0读者 1管理员
    private Integer status;        // 状态：0待审核 1正常 2禁用
    private Integer gender;        // 性别：0女 1男 2保密
    private String openId;         // 微信OpenID
    private String department;     // 所属系部
    private Integer creditScore;   // 信用分
    private Integer todayAddScore; // 今日已加分
    private java.util.Date scoreUpdateDate; // 积分更新日期
}
```

### BookInfo 实体类

```java
package com.shanzhu.book.model;

import lombok.Data;
import java.io.Serializable;
import java.math.BigDecimal;

@Data
public class BookInfo implements Serializable {
    private Integer bookid;
    private String bookname;       // 书名
    private String bookauthor;     // 作者
    private BigDecimal bookprice;  // 价格
    private Integer booktypeid;    // 分类ID
    private String booktypename;   // 分类名称
    private String bookdesc;       // 寄语简介
    private Integer isborrowed;    // 是否借出：0有库存 1无库存
    private String bookimg;        // 封面图片
    private Integer bookcount;     // 总数
    private Integer inventory;     // 剩余库存
    private Integer uploaderid;    // 发布者ID
    private String contactinfo;    // 交接联系方式
    private Integer auditstatus;   // 审核状态：0待审核 1通过 2驳回
    private Integer isSameSex;     // 同性宿区标识
}
```

### Borrow 实体类

```java
package com.shanzhu.book.model;

import com.fasterxml.jackson.annotation.JsonFormat;
import lombok.Data;
import java.util.Date;

@Data
public class Borrow {
    private Integer borrowid;
    private Integer userid;
    private String username;
    private String studentid;
    private Integer bookid;
    private String bookname;

    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
    private Date borrowtime;       // 借阅时间

    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
    private Date returntime;       // 归还时间

    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
    private Date applytime;        // 申请时间

    private String borrowreason;   // 申请理由
    private Integer state;         // 状态
    private String returnmsg;      // 归还/驳回消息
    private String secretCode;     // 交接暗号
    private Integer borrowDays;    // 借阅天数
    private Integer creditScore;   // 借阅者信用分
}
```

---

## 六、借阅状态流转图

```
状态码说明：
├── 0 - 待审核（用户提交申请，等待发布者审核）
├── 1 - 漂流中（交接完成，正在阅读）
├── 2 - 已归还（读完传递给下一位）
├── 3 - 已驳回（发布者拒绝申请）
├── 4 - 待交接（审核通过，等待线下交接）
├── 5 - 已撤销（用户主动取消）
├── 6 - 已遗失（书籍丢失，退出漂流）
└── 7 - 已失效（超时系统自动取消）

状态流转：
[用户申请] → 0(待审核)
     │
     ├── 审核通过 → 4(待交接) → 暗号核销 → 1(漂流中) → 归还传递 → 2(已归还)
     │                  │
     │                  └── 超时/撤销 → 7(已失效)/5(已撤销)
     │
     ├── 审核驳回 → 3(已驳回)
     │
     └── 用户撤销 → 5(已撤销)

[漂流中] → 逾期 → 每日扣分
     │
     ├── 归还传递 → 2(已归还)
     │
     ├── 报损登记 → 6(已遗失) → 扣10分
     │
     └── 强制收回 → 7(已失效) → 扣10分
```

---

## 七、文件路径索引

| 模块 | 后端文件 | 前端文件 |
|------|----------|----------|
| 用户认证 | `代码/book-backend/src/main/java/com/shanzhu/book/web/UserController.java` | `代码/book-frontend/src/views/login/index.vue` |
| 用户服务 | `代码/book-backend/src/main/java/com/shanzhu/book/service/impl/UserServiceImpl.java` | `代码/book-frontend/src/views/user/index.vue` |
| 图书管理 | `代码/book-backend/src/main/java/com/shanzhu/book/web/BookInfoController.java` | `代码/book-frontend/src/views/bookinfo/index.vue` |
| 图书服务 | `代码/book-backend/src/main/java/com/shanzhu/book/service/impl/BookInfoServiceImpl.java` | - |
| 借阅管理 | `代码/book-backend/src/main/java/com/shanzhu/book/web/BorrowController.java` | `代码/book-frontend/src/views/borrow/index.vue` |
| 借阅服务 | `代码/book-backend/src/main/java/com/shanzhu/book/service/impl/BorrowServiceImpl.java` | - |
| 定时任务 | `代码/book-backend/src/main/java/com/shanzhu/book/task/BorrowTask.java` | - |
| 首页仪表盘 | - | `代码/book-frontend/src/views/dashboard/index.vue` |
| 用户实体 | `代码/book-backend/src/main/java/com/shanzhu/book/model/User.java` | - |
| 图书实体 | `代码/book-backend/src/main/java/com/shanzhu/book/model/BookInfo.java` | - |
| 借阅实体 | `代码/book-backend/src/main/java/com/shanzhu/book/model/Borrow.java` | - |

---

**文档生成时间：** 2026-03-27

**技术栈：** Spring Boot 2.5.6 + MyBatis + MySQL 8.0 + Vue 2.x + Element-UI + WxPusher
